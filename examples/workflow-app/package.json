{
  "name": "@ingcreators/workflow-app-example",
  "version": "0.0.0",
  "private": true,
  "description": "Workflow approval SPA example exercising @ingcreators/annot-product-docs + -astro end-to-end. Standalone — NOT a workspace package; cd here and run `npm install` before `npm run dev`. See docs/plans/_done/workflow-app-example.md for the build history.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "docs:sync": "playwright test"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "@ingcreators/annot-product-docs": "^0.3.0",
    "@ingcreators/annot-product-docs-astro": "^0.3.0",
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
