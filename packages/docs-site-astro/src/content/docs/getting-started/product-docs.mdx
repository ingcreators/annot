---
title: Install the living-product-docs trio
description: annot-product-docs + -astro + -xlsx — generate a docs site and a Japanese 画面設計書 from the same MDX, kept in sync with Playwright snapshots.
---

The living-product-docs packages turn a Playwright tour suite
into always-fresh user manuals, **and** Excel 画面設計書
(screen specs) — same MDX source, dual rendering targets, drift
between MDX + live UI caught as a CI lint step.

Three packages compose into the flow:

| Package | Role |
|---|---|
| [`@ingcreators/annot-product-docs`](../api/product-docs) | Core: MDX parser, Playwright `screen` fixture, drift detector, `annot docs` CLI |
| [`@ingcreators/annot-product-docs-astro`](../api/product-docs-astro) | Astro components (`<Screen>` / `<Overlay>` / …) + the Image Service that composes annotated PNGs at build time |
| [`@ingcreators/annot-product-docs-xlsx`](../api/product-docs-xlsx) | Excel adapter — emits one `.xlsx` per book using either the OSS default layout or a customer-supplied template populated via `{var}` placeholders + Named Ranges |

## Install

```sh
pnpm add @ingcreators/annot-product-docs
pnpm add @ingcreators/annot-product-docs-astro      # if you want a docs site
pnpm add @ingcreators/annot-product-docs-xlsx       # if you want 画面設計書 Excel output

# Playwright is a peer dep of the core package.
pnpm add -D @playwright/test
```

## Scaffold

```sh
pnpm annot-docs init
```

Drops three files into the repo:

```
annot-docs.config.ts              # project + book config
tests/docs/example.spec.ts        # Playwright tour
docs/books/example/SC-001-login.mdx  # one screen, three overlays
```

## Author one screen

```mdx
---
annot:
  id: SC-001
  title: Login screen
  xlsx:
    book: Screen spec
    sheet: SC-001 Login
    role: screen
---

import Screen from "@ingcreators/annot-product-docs-astro/components/Screen.astro";
import Overlay from "@ingcreators/annot-product-docs-astro/components/Overlay.astro";

# Login screen

<Screen id="login" src="./shots/login.png">

<Overlay match={{ role: "textbox", name: "Email" }} intent="required" number={1}>
**Email** — Enter your registered email.
</Overlay>

<Overlay match={{ role: "button", name: "Sign in" }} intent="action" number={2}>
Click to sign in.
</Overlay>

</Screen>
```

`match` keys are persistent — Playwright's `ref=eN` markers in
aria snapshots are not. The resolver re-finds each overlay's
target element on every tour run.

## Wire up the tour

```ts
// tests/docs/auth.spec.ts
import { test } from "@ingcreators/annot-product-docs";

test("login flow", async ({ page, screen }) => {
  await page.goto("/login");
  await screen.capture({
    id: "login",
    mdxPath: "docs/books/example/SC-001-login.mdx",
  });
});
```

`screen.capture(...)` updates the `annot:snapshot` +
`annot:attributes` comment blocks in the MDX in-place, byte-stably
— diffs are stable across reruns when the UI hasn't changed.

## Render the docs

```sh
# Astro docs site
pnpm --filter docs-site build

# Excel 画面設計書
pnpm annot-docs-xlsx render
```

## Lint for drift in CI

```sh
annot-docs lint --ci --json --url http://localhost:5173
```

Six finding kinds — added / removed / renamed / role-changed /
duplicated / attribute-drift. The example GitHub Actions
workflow shipped with the package converts the JSON to per-line
`core.warning` annotations on the PR diff view.

## Next steps

- [Concepts](../product-docs/concepts) — the MDX format,
  `<Screen>` / `<Overlay>`, snapshot blocks, attributes.
- [Playwright tour](../product-docs/playwright-tour) — full
  walkthrough of the tour file shape + the `screen` fixture.
- [Astro integration](../product-docs/astro-integration) —
  `productDocsIntegration()`, the Image Service, the seven
  components.
- [Xlsx templates](../product-docs/xlsx-templates) — placeholder
  vocabulary + Named Ranges for customer template authoring.
- [Drift detection](../product-docs/drift-detection) — the lint
  flow + the bundled GitHub Actions workflow.
- [Recipe: living product docs](../recipes/living-product-docs) —
  end-to-end working example.
