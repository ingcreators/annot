/**
 * Public root entry — `@ingcreators/annot-doc`.
 *
 * Mirror of `@ingcreators/annot-core` root: indistinguishable from
 * the `headless` subpath at v1 because the entire Phase 1 surface
 * is Tier A. Future phases (Phase 4 inline AST, Phase 7 template
 * helpers) may add Tier B / C subpaths under their own export
 * conditions.
 */

export * from "./headless.js";
