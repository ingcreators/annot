{
  "name": "@ingcreators/annot-extension",
  "version": "0.1.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ingcreators/annot.git",
    "directory": "packages/extension"
  },
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.ts && node scripts/build-manifest.mjs",
    "build:dev": "vite build --mode development && vite build --mode development --config vite.content.config.ts && node scripts/build-manifest.mjs",
    "build:chrome": "vite build && vite build --config vite.content.config.ts && node scripts/build-manifest.mjs",
    "build:edge": "vite build && vite build --config vite.content.config.ts && node scripts/build-manifest.mjs",
    "package:chrome": "pnpm build:chrome && node scripts/zip-dist.mjs chrome",
    "package:edge": "pnpm build:edge && node scripts/zip-dist.mjs edge",
    "dev": "vite",
    "e2e": "pnpm build:dev && playwright test",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tests/e2e",
    "icons": "node ../../brand/generate-app-icons.mjs"
  },
  "dependencies": {
    "@ingcreators/annot-capture": "workspace:*",
    "@ingcreators/annot-core": "workspace:*",
    "@ingcreators/annot-editor": "workspace:*",
    "@ingcreators/annot-host-ui": "workspace:*",
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^26",
    "@storybook/web-components-vite": "^10.5.2",
    "chrome-types": "^0.1.435",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
