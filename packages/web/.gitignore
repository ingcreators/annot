# Generated by `appVersionPlugin` in `vite/version-plugin.ts` on
# every `vite dev` / `vite build`. Contents reflect the current
# commit SHA (CI / local) or a `dev-<timestamp>` sentinel.
# Committing it would just produce churn on every build.
public/version.txt
