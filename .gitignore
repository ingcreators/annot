node_modules/
dist/
storybook-static/
.turbo/
*.tsbuildinfo

# Rust
target/
Cargo.lock
# Reproducible builds for the in-tree libimagequant wrapper need a
# committed lockfile (cargo --locked). The leaf-crate scope means
# this exception is safe; the desktop tauri crate keeps its lockfile
# regenerated.
!packages/imagequant/Cargo.lock

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Environment
.env
.env.local

# Claude Code (local-only)
.claude/settings.local.json
.claude/worktrees/
