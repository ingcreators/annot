# Living-spec visual authoring roadmap

> **Status:** Draft — written 2026-05-22 as the synthesis of a long
> design discussion that started from
> [`playwright-screenshot-fixture-relayer.md`](./playwright-screenshot-fixture-relayer.md)'s
> Q&A about Playwright codegen compatibility and walked through six
> architectural alternatives before landing on a 3-layer model.
>
> **Trigger:** The relayer plan's review surfaced that
> `@ingcreators/annot-product-docs-astro/playwright`'s
> `page.screenshot({ annot: { mdx } })` patch is not actually
> Astro-specific. That observation cascaded into a re-examination
> of how the whole living-product-docs surface is layered, and
> revealed that the editor → MDX → PNG triangle is currently
> ambiguous about who owns what. This roadmap captures the
> resulting positioning decision: **Annot becomes the visual
> authoring surface for living product docs**, with a strict
> 3-layer artifact model and a 5-phase implementation path that
> ends with embedded in-browser editing committing back to GitHub.
>
> **Compatibility:** Additive throughout. Existing `<Overlay>` JSX
> migrates via deterministic CLIs. Existing `screen.capture(...)`
> stays as a documented escape hatch. The PNG XMP format gains
> new optional fields. No `data-annot-version` bump.
>
> **Risk:** Medium. Each phase is independently shippable; the
> architectural commitments are well-reasoned but cover a lot of
> surface. Phase 5 (embedded editor + GitHub round-trip) lives
> partially in `annot-cloud` (private repo) and depends on the
> annot-cloud auth foundation in
> [`annot-cloud-roadmap.md`](./annot-cloud-roadmap.md) Phase 3.

## TL;DR

After
[`_done/living-product-docs.md`](./_done/living-product-docs.md)
landed the docs platform (MDX + Playwright fixture + Astro
adapter + Excel adapter), one foundational question stayed open:
**how do people without IDE access edit living-product-docs?**
Today the answer is "open the MDX file in VSCode, type JSX." This
roadmap replaces that with: open the PNG in Annot, click an
element, type text, save. Either locally (Annot desktop /
VSCode), in a hosted editor (annot.work/app), or — as the
terminal ambition — **inline inside the rendered docs site
itself, with edits committing back to GitHub**.

The journey requires re-thinking the artifact model. Today's
`<Overlay match={...} intent number>text</Overlay>` JSX conflates
**three different things** with three different lifecycles:

| Concern | Lifecycle | Editor | Today's home |
|---|---|---|---|
| Image + aria-snapshot | Re-captured on UI change | Playwright tour | PNG + MDX `annot:snapshot` block |
| Visual annotation (numbered badge / arrow / rect / etc.) | Edited when "what to point at" changes | Annot editor (proposed) | `<Overlay>` JSX `match` / `intent` / `number` props |
| Description text | Edited per-audience, translated per-language | Technical writer / PM | `<Overlay>` JSX children |

The roadmap separates these into three layers, each with its
own storage and editor:

- **Layer 1**: PNG with `annot:snapshot` / `annot:attributes` in XMP. Machine-generated by Playwright, regenerated on re-capture.
- **Layer 2**: `.annotations.yaml` files referenced from MDX via `<Screen annotations="...">`. Machine-resolvable (each entry has `match: { role, name }` or free coords). Edited via Annot editor's new Overlay tool.
- **Layer 3**: `<Description for="id">text</Description>` JSX inside MDX. Rich markdown / JSX content, per-audience, translatable. Edited in any MDX editor.

This separation enables:

1. **Annot stays a general PNG annotator** — the docs use case is one of many.
2. **MDX retains markdown / JSX expressiveness** for description body.
3. **PNG is re-capture-safe** — overlays don't live in PNG, so overwriting PNG doesn't destroy human work.
4. **Multi-book / multi-audience** — same PNG + annotation yaml, different MDX descriptions.
5. **Embedded editing** — annot.work iframe can mount in any docs page and round-trip edits via GitHub Storage.

## Strategic positioning

Today's competitive landscape:

| Product | Visual editor | Docs output | Embedded editing |
|---|---|---|---|
| VitePress / Mintlify | None (markdown only) | Yes | None (PRs only) |
| Notion | Rich text | Yes | Yes (in-app, not git) |
| Confluence | Rich text | Yes | Yes (in-app, not git) |
| Excalidraw | Free-form drawing | Limited | Limited |
| Figma | UI design | Read-only | Comments only |
| **Annot (after this roadmap)** | **Element-aware visual** | **Yes** | **Yes (git-backed)** |

The combination — visual editor + element-aware annotations + git
round-trip — has no existing competitor. Mintlify is closest but
lacks visual editing; Notion is closest on UX but isn't
git-backed.

This positions Annot as **the canonical editor for screen
specifications** (画面仕様書) — a category that exists in
Japanese enterprise tech especially heavily (operation manuals,
screen design documents, regression-test specs), and that
currently lives in Excel / PowerPoint / Confluence with no
modern tooling.

The OSS / Cloud split per
[`oss-cloud-split.md`](./oss-cloud-split.md):

- **OSS** (`annot` repo): Annot editor + product-docs core +
  Astro adapter + Excel adapter + annotation yaml format + local
  filesystem / GitHub storage
- **annot-cloud** (private repo): hosted editor at `annot.work/app/`,
  iframe embed mode, GitHub App for server-side commits, build
  trigger integration, multi-tenant DB, billing

Phase 5 of this roadmap straddles both repos.

## The 3-layer artifact model

```
┌──────────────────────────────────────────────────────────────────┐
│ Layer 1 — Image (machine-generated, re-captured)                 │
│                                                                  │
│   docs/shots/login.png                                           │
│     ├── image bytes                                              │
│     ├── PNG XMP: annot:snapshot   (aria tree + [box=...])        │
│     └── PNG XMP: annot:attributes (HTML attrs for overlay-touched)│
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ <Screen src="login.png">
                              │
┌──────────────────────────────▼───────────────────────────────────┐
│ Layer 2 — Annotations (visual marking, human-driven)              │
│                                                                   │
│   docs/annotations/login.annotations.yaml                         │
│     version: 1                                                    │
│     overlays:                                                     │
│       - id: o1                                                    │
│         kind: numberedBadge                                       │
│         match: { role: "textbox", name: "Email" }                 │
│         intent: required                                          │
│         number: 1                                                 │
│       …                                                           │
│     annotations:                                                  │
│       - id: a1                                                    │
│         kind: arrow                                               │
│         from: { match: ... }                                      │
│         to:   { match: ... }                                      │
│         intent: action                                            │
└──────────────────────────────┬───────────────────────────────────┘
                              │ <Screen annotations="login.annotations.yaml">
                              │
┌──────────────────────────────▼───────────────────────────────────┐
│ Layer 3 — Descriptions (rich text, per-audience, translatable)    │
│                                                                   │
│   docs/books/operation-manual/ja/OM-001-login.mdx                 │
│     <Screen                                                       │
│       src="../../shots/login.png"                                 │
│       annotations="../../annotations/login.annotations.yaml"      │
│     >                                                             │
│       <Description for="o1">                                      │
│         登録済みのメールアドレスを入力してください。              │
│       </Description>                                              │
│       <Description for="o2">                                      │
│         パスワードを入力してください。`password` で…              │
│       </Description>                                              │
│     </Screen>                                                     │
└──────────────────────────────────────────────────────────────────┘
```

### Why three layers, not one or two

The 6 alternatives considered during the design discussion (and
why each fails) are captured in
[Discarded alternatives](#discarded-alternatives) at the bottom.
The short version:

- **One file (PNG with everything)**: re-capture overwrites PNG, destroying human overlays.
- **Two files with PNG sidecar (`.overlays.yaml` paired with PNG by filename)**: pair fragility — move PNG without yaml, data lost.
- **Two files with MDX inline (current state)**: requires MDX JSX writer for the editor (highest implementation cost), conflates structure/text concerns.
- **Three files (this proposal)**: explicit link in MDX prevents pair fragility, separation of concerns is clean, no MDX JSX writer needed.

The third layer (Description in MDX) is non-negotiable per the
user's feedback during the session: description text wants
markdown formatting, inline JSX components (`<code>`,
`<KeyboardKey>`, `<Note>`, etc.), and integration with existing
translation pipelines. YAML for description is over-extraction.

### Why annotation yaml is referenced explicitly, not paired by filename

```mdx
<Screen
  src="../shots/login.png"
  annotations="../annotations/login.annotations.yaml"  ← explicit
/>
```

The `annotations` prop is non-optional once Layer 2 is in use.
If MDX moves, the prop's relative path needs updating just like
`src`. If the yaml file goes missing, **the Astro build fails
loudly** instead of silently rendering without overlays.

Pairing by filename convention (e.g. `login.png` →
`login.overlays.yaml` in same dir) was rejected because:

1. **File moves break silently**: `mv login.png shots/old/` doesn't bring the sidecar.
2. **Sharing strips the pair**: PNG dropped into Slack / email arrives without yaml.
3. **No build-time validation**: missing yaml silently degrades to "no overlays" output.

Explicit reference makes the link a first-class MDX dependency,
verifiable at build time and visible in code review.

### How the editor never touches MDX

A central simplification: **Annot editor's save flow writes only
the annotation yaml**. Never the MDX. Never the PNG XMP (except
for tour-generated snapshot data, which the editor itself
doesn't write).

This eliminates the MDX JSX writer that earlier iterations of
this design required. MDX JSX is **read-only** to the editor —
the editor opens MDX to discover linked yaml paths, then edits
the yaml.

Editor implementation surface shrinks dramatically:

| Capability | Implementation |
|---|---|
| Read PNG XMP snapshot | Existing `@ingcreators/annot-core/xmp-bytes` |
| Display snapshot regions as clickable | New (parses YAML → Lit overlay component) |
| Add / move / edit annotation | New (modifies in-memory yaml model) |
| Save annotation yaml | New (Tier A YAML serializer, ~50 lines) |
| Open via MDX context | New (MDX → `<Screen annotations>` lookup) |
| Direct PNG open | New (no MDX context, no edit; read-only) |

**No remark-mdx walking, no JSX node insertion, no byte-stable
markdown rewriter.**

### Annotation type palette

Layer 2 yaml accommodates Annot's full annotation palette, not
just numbered badges:

```yaml
version: 1

overlays:
  # numbered callouts (the docs-primary use case, matches today's <Overlay>)
  - id: o1
    kind: numberedBadge
    match: { role: "textbox", name: "Email" }
    intent: required
    number: 1

annotations:
  # arrows between elements
  - id: a1
    kind: arrow
    from: { match: { role: "button", name: "Sign in" } }
    to:   { match: { role: "main" } }
    intent: action

  # rect bounding multiple elements
  - id: a2
    kind: rect
    coversElements:
      - { match: { role: "textbox", name: "Email" } }
      - { match: { role: "textbox", name: "Password" } }
    intent: info

  # text annotation anchored to element
  - id: a3
    kind: text
    anchor: { match: { role: "heading", name: "Sign in" }, position: above }
    text: 未認証ユーザのエントリポイント

  # free-form annotations (UI changes may misalign these)
  - id: a4
    kind: freehand
    path: "M100,200 L150,250 L200,210"
    stroke: "#ff0000"

  - id: a5
    kind: redact
    bbox: { x: 421, y: 269, width: 438, height: 40 }
    style: solid

  - id: a6
    kind: focusMask
    cutout: { match: { role: "button", name: "Sign in" }, padding: 8 }
    intent: action
```

Two top-level sections:

- `overlays[]` — numbered badges, the docs-primary numbered-callout case. Always match-anchored. Has `text` if Description is inline (transition state during migration), or has `description: false` to signal "no description, just a numbered marker."
- `annotations[]` — everything else: arrows, rects, text labels on the image, freehand, redact, focus mask, etc. May be match-anchored, may be free-coord.

The split is by **whether the docs flow renders a description
slot for it**. `overlays[]` entries get a description rendered
below the image; `annotations[]` entries are pure visual marking
with no description slot (though they can carry an `intent` for
styling).

`<Description for="id">` JSX in MDX targets `overlays[]` IDs.
`annotations[]` IDs are never referenced from MDX — they're
self-contained visual marking.

### Match resolution and drift

Both `match: { role, name }` and free-coord (`bbox` / `position`
/ `path`) are supported. Match-anchored entries:

- Survive UI layout changes (the snapshot resolver picks them up by role+name)
- Subject to drift detection (`annot-docs lint` flags missing matches)
- Coordinates computed at build time from snapshot's `[box=...]` markers

Free-coord entries:

- Immune to drift detection (no match to verify)
- Brittle to UI changes (pixel coordinates don't track layout)
- Used for decorative annotations, freehand sketches, redact regions

Element-anchoring is the preferred default. Annot editor's UI
nudges users toward it: dragging an annotation near a snapshot
element offers a "snap to element" affordance that converts
free-coord to `match`-based.

## Phase plan

The roadmap spans 6 phases (counting the already-Draft relayer
plan as Phase 0). Each phase is independently shippable and
revertable; collectively they implement the 3-layer model.

| Phase | Output | Estimate | Repo |
|---|---|---|---|
| 0 | Architecture relayer | ~4 small PRs | annot |
| 1 | PNG XMP snapshot/attributes cache | ~3 PRs | annot |
| 2 | Annotation yaml extraction + `<Description>` JSX | ~5 PRs | annot |
| 3 | Annotation palette extension (arrows / rect / freehand / etc.) | ~4 PRs | annot |
| 4 | Annot editor's Overlay tool + yaml writer | ~6 PRs | annot |
| 5 | Embedded editor + GitHub round-trip | ~8 PRs | annot + annot-cloud |

Cumulative implementation: ~30 PRs across both repos. Each phase
delivers user-visible value standalone.

### Phase 0 — Architecture relayer (Draft)

Already captured in
[`playwright-screenshot-fixture-relayer.md`](./playwright-screenshot-fixture-relayer.md)
([PR #955](https://github.com/ingcreators/annot/pull/955)).

Moves `page.screenshot({ annot: { … } })` prototype patch from
`@ingcreators/annot-product-docs-astro/playwright` to its proper
homes:

- `@ingcreators/annot-playwright` gains the generic patch (overlays / tags / editable / clip rebasing) plus a module-level `annotSourceResolvers` hook registry
- `@ingcreators/annot-product-docs` extends the registry with the `mdx` resolver
- `@ingcreators/annot-product-docs-astro/playwright` becomes a deprecated re-export

**Why this is Phase 0 of the roadmap**: the hook-registry pattern
introduced here is the foundation for Phase 2's `<Description>`
extension and Phase 3's annotation-palette additions. Without
the registry, every new field on `AnnotScreenshotOptions` would
require modifying the top-level patch — exactly the wrong shape
for a multi-phase evolution.

### Phase 1 — PNG XMP snapshot/attributes cache

**Goal**: Move `annot:snapshot` and `annot:attributes` blocks
from MDX comment blocks to PNG XMP. MDX retains its blocks for
backward compatibility during transition.

**Files added**:

- `packages/core/src/xmp-bytes/snapshot-fields.ts` — Tier A. Type definitions for `SnapshotXmp` / `AttributesXmp` payloads + reader / writer functions. Reuses existing iTXt encoder.
- `packages/playwright/src/snapshot-writer.ts` — fixture writes PNG XMP at capture time, populates `snapshot` / `attributes` fields from the same `ariaSnapshot()` call that today writes to MDX comment blocks.

**Files modified**:

- `packages/product-docs/src/fixture.ts` — `captureScreen()` writes BOTH PNG XMP (new) AND MDX comment blocks (existing). Dual-write during transition.
- `packages/product-docs-astro/src/render.ts` — `renderAnnotatedScreen()` reads snapshot from PNG XMP if present, falls back to MDX comment block. Build-time logging notes which source was used.

**CLI additions**:

- `annot-docs migrate-snapshots-to-png` — walk all `<Screen src>` references in MDX bundle. For each, find the PNG, copy its `annot:snapshot` and `annot:attributes` content from the MDX comment block to PNG XMP. Idempotent.

**Schema versioning**: PNG XMP gets `snapshot-version: 1` and
`attributes-version: 1` fields so future schema changes can be
detected.

**Verification**: dual-write means existing MDX builds unchanged.
Migration CLI tested with workflow-app's 17 MDX files. After
migration, deleting MDX comment blocks should produce
byte-identical Astro builds (snapshot read from PNG instead).

**Out of scope for Phase 1**: removing MDX comment block
support. That happens in a follow-up after the transition
period (1-2 release cycles).

### Phase 2 — Annotation yaml extraction + `<Description>` JSX

**Goal**: Move `match` / `intent` / `number` from `<Overlay>`
JSX props into `.annotations.yaml` files. Replace `<Overlay>`
JSX with `<Description for="id">body</Description>` for the
description text.

**Files added**:

- `packages/product-docs/src/annotations-yaml.ts` — Tier A. Type definitions for `OverlayEntry` (`id` / `kind: numberedBadge` / `match` / `intent` / `number`). Parser + serializer (`js-yaml` already in deps).
- `packages/product-docs-astro/src/components/Description.astro` — new JSX component matching annotation by `for` prop. Children are markdown / JSX, rendered below the annotated image with intent-colored callout border (matching the screenshot the user shared during the design discussion).

**Files modified**:

- `packages/product-docs-astro/src/components/Screen.astro` — accepts new `annotations` prop pointing at the yaml file. Reads yaml + composes with snapshot bboxes + base PNG to produce the annotated PNG. Renders `<Description>` children in order matching `overlays[].number`.
- `packages/product-docs/src/types.ts` — `OverlaySpec` deprecated. Replaced by `AnnotationSpec` (in yaml) + `DescriptionSpec` (in MDX, just `id` + children).
- `packages/product-docs/src/drift.ts` — drift detection extends to validate annotation yaml against live snapshot AND validates MDX `<Description for>` IDs against annotation yaml `overlays[].id`.

**CLI additions**:

- `annot-docs migrate-overlays-to-annotations` — walk MDX, find every `<Overlay match intent number>body</Overlay>`. For each, generate an entry in the screen's annotation yaml file (one yaml per `<Screen src>`). Replace the JSX in MDX with `<Screen annotations="..."><Description for="id">body</Description></Screen>`.
- `annot-docs lint --check-descriptions` — verify every `<Description for>` matches an annotation yaml ID. Verify every `overlays[].id` has a description. Configurable severity per direction.

**Schema versioning**: annotation yaml carries `version: 1`. Parser version-dispatches.

**Migration path**: dual support during transition — `<Overlay>`
JSX continues to work (parsed by existing resolver) AND
`<Screen annotations>` + `<Description>` works. After a
deprecation period, `<Overlay>` support is removed.

**Verification**: workflow-app's existing `<Overlay>` count
(17 MDX × ~5 overlays each = ~85 overlays) all migrate
deterministically. Pixel-identical Astro output before and
after migration.

### Phase 3 — Annotation palette extension

**Goal**: Extend annotation yaml schema with the full Annot
palette beyond numbered badges. Add `annotations[]` section
(distinct from `overlays[]`).

**Files added**:

- `packages/product-docs/src/annotations-yaml.ts` (extended from Phase 2) — adds `AnnotationSpec` variants: `rect`, `arrow`, `text`, `callout`, `freehand`, `redact`, `focusMask`, `circle`.
- `packages/product-docs-astro/src/render-annotations.ts` — extends `renderAnnotatedScreen` to compose `annotations[]` entries onto the base PNG, on top of the `overlays[]` numbered badges.
- `packages/annotator/src/dsl-annotations-yaml.ts` — yaml → `BboxAnnotation[]` mapper. The existing `bboxAnnotationsToSvg` DSL in `@ingcreators/annot-annotator` handles all the rendering once we map yaml entries to `BboxAnnotation` union members.

**Files modified**:

- `packages/product-docs/src/drift.ts` — drift detection scope: match-anchored `annotations[]` entries participate; free-coord entries are skipped.
- `packages/product-docs-xlsx/src/extract.ts` — extends Excel extraction: `overlays[]` populates the "項目定義" table (already today's behavior); `annotations[]` is rendered into the image-only column without text rows.

**Element anchoring vs free-coord**:

- Match-anchored is the default for `rect` / `arrow` / `text` / `callout` / `circle` / `redact` / `focusMask` when the entry has a `match` / `coversElements` / `anchor` field.
- Free-coord (`bbox` / `position` / `path`) is the escape hatch for `freehand`, decorative `text`, and any annotation that doesn't correspond to an aria element.
- Editor's "snap to element" UX (added in Phase 4) nudges users toward match-anchored when possible.

**Verification**: Storybook stories for each annotation kind
demonstrating composition with a base PNG. Snapshot tests in
`packages/product-docs-astro/src/render-annotations.test.ts`.

### Phase 4 — Annot editor's Overlay tool + yaml writer

**Goal**: Annot editor (`packages/editor` + `packages/host-ui`)
gains the visual editing surface that produces annotation yaml
output.

**Files added**:

- `packages/editor/src/tools/overlay-tool.ts` — new tool. Hover over a snapshot element → highlight. Click → opens "Add Overlay" dialog (intent picker + number auto-assigned).
- `packages/host-ui/src/annotation-yaml-loader.ts` — loads annotation yaml from the storage layer associated with the host (`DeviceStore` / `VSCodeStore` / `GitHubStore`).
- `packages/host-ui/src/annotation-yaml-writer.ts` — atomic write of annotation yaml back to storage. No PNG XMP write. No MDX write.
- `packages/host-ui/src/snapshot-overlay.ts` — Lit component that reads PNG XMP snapshot, renders the `[box=...]` regions as semi-transparent overlay rectangles for hover/click affordance.

**Files modified**:

- `packages/editor/src/canvas-manager.ts` — registers the new tool. Tool palette UI gets a new "Overlay" button distinct from the existing rect/arrow/callout.
- `packages/host-ui/src/editor-shell.ts` — `EditorShell.mountFromRecord` accepts an optional `annotationsYamlPath` parameter. When set, the shell loads the yaml on mount and saves to it on every annotation edit.

**Storage adapter changes**:

- `StorageWithDocuments` capability extended with `getAnnotationsYaml(pngPath) → string` and `setAnnotationsYaml(pngPath, content)` methods. Optional on the base interface; feature code checks `if (store.getAnnotationsYaml)`.
- `DeviceStore` / `DesktopStore` / `VSCodeStore` / `GitHubStore` / `GoogleDriveStore` each implement the new methods.

**UX flow**:

```
1. User opens login.png in Annot editor (any host)
2. Editor detects PNG XMP snapshot → shows hover hotspots
3. Editor detects (via MDX context or PNG XMP "annotations-ref" hint) the linked yaml path
4. Editor loads yaml → renders existing overlays + annotations as visual marks
5. User clicks "Overlay" tool → clicks Email textbox → adds overlay
   - Annot proposes match={ role: "textbox", name: "Email" } from the clicked element
   - User picks intent (required / action / info)
   - Number auto-assigned (next available)
6. Save → yaml is rewritten with new entry. PNG untouched. MDX untouched.
```

**Verification**: Storybook stories cover the tool's modes
(hovering / clicking / editing existing). Vitest tests cover the
yaml writer's idempotency and atomicity.

### Phase 5 — Embedded editor + GitHub round-trip

**Goal**: Annot editor mountable inline in any docs site,
edits commit back to GitHub via annot-cloud's GitHub App
integration.

**Files added (annot repo, OSS side)**:

- `packages/product-docs-astro/src/components/EditButton.astro` — MDX-mountable component. Renders an "✏️ Edit" button. On click, opens the annot.work editor in an iframe (modal or inline).
- `packages/product-docs-astro/src/components/AnnotEditorEmbed.astro` — wraps the iframe with sizing, focus, and postMessage handling.
- `packages/annot-embed-protocol/` (new workspace package) — Tier A. Defines the postMessage protocol between the parent (docs site) and the iframe (annot.work editor). Messages: `EditorReady` / `EditRequested` / `EditCommitted` / `EditAbandoned` / `ResizeNeeded`.

**Files added (annot-cloud repo, paid side)**:

- `embed/` route in annot.work — receives URL params (`repo` / `pngPath` / `annotationsPath` / `return`), reads from GitHub via GitHub App credentials, mounts an editor instance, sends `EditorReady` postMessage to parent.
- GitHub App "annot-cloud-editor" — installable on customer repos. Permissions: contents (read+write), metadata (read), pull_requests (write, for PR-mode edits).
- Worker route in annot-cloud Workers that proxies the GitHub commit (so the editor doesn't need a user PAT — only the GitHub App's installation token).
- Build trigger integration — after commit, the Worker pings the customer's Cloudflare Pages / Vercel / GitHub Pages build endpoint (configured per-installation).

**UX flow**:

```
Public docs site (annot.work/docs or customer site)
  ↓
Visitor sees <Screen> with annotated PNG + descriptions
  ↓
"✏️ Edit" button (only if EditButton component present + repo has annot-cloud App installed)
  ↓
Click → iframe opens annot.work/embed?repo=...&png=...&yaml=...
  ↓
Authentication: GitHub OAuth in iframe (if not already signed in)
  ↓
Editor loads PNG + yaml from GitHub via GitHub App
  ↓
User edits visual annotations
  ↓
"Save" → annot-cloud commits to GitHub:
  - Option A: direct push to main
  - Option B: PR to main from a branch
  - Configured per-installation
  ↓
Build trigger fires → site rebuilds in 30s-2min
  ↓
postMessage notifies parent → "Edit saved, refresh in ~1 minute"
```

**Conflict handling**: simple "last write wins" in v1. Future:
optimistic locking via GitHub's `If-Match` ETag header.
Documented as a known limitation.

**Permissions model**:

- **Anonymous viewer**: sees Edit button if EditButton is rendered, click prompts GitHub sign-in
- **Signed-in viewer without repo write**: editor opens in read-only mode (still useful for previewing annotations) + offers "Suggest changes via PR" flow
- **Signed-in with repo write**: full edit, commits to configured target (main or branch)
- **Repo owner**: configures per-installation policy in annot-cloud dashboard

**Pricing tier mapping** (per
[`annot-cloud-roadmap.md`](./annot-cloud-roadmap.md)):

- Free hosted (annot.work/app) — visitors can edit their own repos, no embedding allowed in third-party sites
- Pro ($5/mo) — embed in personal sites, public repos only
- Team ($5/user/mo) — embed in private repos, audit log
- Enterprise — SSO, custom GitHub App, SLA

**Verification**: integration tests in annot-cloud staging
environment exercising the full GitHub round-trip against a
test repo. Manual QA covering the conflict + permissions
matrix.

**Out of scope for Phase 5**:

- Real-time collaboration (multiple editors editing the same file simultaneously). Defer to a future plan.
- Mobile-friendly editor — desktop-only in v1.
- Inline editing of `<Description>` MDX bodies. The embedded editor only edits Layer 2 (annotation yaml). Description editing requires opening MDX in IDE or future MDX editor.

## Architectural decisions

### AD-01: Annotation yaml is referenced explicitly from MDX, not paired by filename

Pairing breaks under filesystem operations (move / rename /
copy). Explicit `annotations="..."` prop in `<Screen>` makes
the dependency:

- **Visible** in code review
- **Verifiable** at build time (missing file → Astro build error)
- **Editable** as a normal MDX attribute when files move
- **Lintable** via `annot-docs lint`

### AD-02: Description body lives in MDX, not yaml

YAML can't natively express:

- Markdown formatting (bold, italic, code, lists, links)
- Inline JSX (`<Link>`, `<Note>`, `<KeyboardKey>`, custom components)
- Multi-paragraph structure with semantic markup

MDX is the right home. Annot editor never touches MDX —
description editing is a separate workflow handled by IDE / MDX
editors / future Annot Cloud MDX editor.

### AD-03: PNG carries snapshot, not overlays

Snapshot is re-generated on every Playwright tour. PNG is
overwritten on re-capture. If overlays lived in PNG, they'd be
destroyed on every CI run.

Annotation yaml lives in a separate file, never overwritten by
re-capture. PNG XMP is purely machine-generated metadata.

### AD-04: Annot editor never edits MDX

The previous design iteration assumed Annot would write MDX
overlay JSX. This required a byte-stable JSX writer (high
implementation cost, remark-mdx limitations).

Separating annotation yaml from MDX descriptions eliminates this
requirement: editor writes only yaml. MDX is read-only to the
editor.

### AD-05: Match-anchored positions are first-class, free-coord is escape hatch

Match-anchored annotations survive UI layout changes. Free-coord
annotations break. The schema prefers match where possible:

- `rect` with `coversElements: [match, match]` — preferred
- `rect` with `bbox: { x, y, w, h }` — escape hatch

Editor UX (Phase 4) nudges users toward match-anchored
placements.

### AD-06: `overlays[]` and `annotations[]` are separate sections

`overlays[]` entries get a description rendered below the image
(via `<Description for="id">` MDX JSX).

`annotations[]` entries are purely visual marking, no description
slot.

Split by **whether the docs flow renders a description slot for
it**, not by annotation kind. A numbered badge could in principle
be `kind: numberedBadge` under either section; the section
choice signals authoring intent.

### AD-07: Hook registry, not configurable fixture

Phase 0's relayer introduces module-level
`annotSourceResolvers` registry. Subsequent phases (2-5) add new
resolvers to this registry: MDX resolver (Phase 0), annotation
yaml resolver (Phase 2), free annotation resolver (Phase 3),
etc.

Single prototype patch, multiple contributors, tree-shake
friendly.

### AD-08: OSS / Cloud split via iframe boundary

Phase 5's embedded editor lives behind an iframe. The OSS side
ships only the `<EditButton>` / `<AnnotEditorEmbed>` components
that mount the iframe. The actual editor + GitHub App + commit
logic lives in annot-cloud.

This keeps:

- OSS docs sites lightweight (no bundled editor)
- annot-cloud as a clear paid service
- Security boundary explicit (GitHub credentials never enter
  the parent docs site's JS context)

## Open Questions

### OQ-01: Annotation yaml schema versioning policy

`version: 1` is in the schema, but the policy for v2 / v3 is
open:

- **(a) Strict version dispatch**: parser reads `version`, picks code path. Old code refuses new versions.
- **(b) Best-effort forward compat**: parser ignores unknown fields, processes known ones. Old code partially handles new versions.

**Recommended:** (a) for the v1 → v2 transition (likely breaking
shape changes); (b) within a major version (e.g. v1.0 → v1.1
adds optional fields).

### OQ-02: Multi-book overlay sets — same annotation, different audience

workflow-app's operation-manual and screen-design books point
the same `login.png` at different `<Description>` content.

Two options:

- **(a) Single annotation yaml shared between books, descriptions diverge** — both books use the same `login.annotations.yaml`, each MDX file has its own `<Description>` set.
- **(b) Per-book annotation yaml** — `login.OM.yaml`, `login.SD.yaml`. Allows different numbering / different intent per book.

**Recommended:** (a). Audience differences are in description
text, not visual marking. Shared yaml encourages consistency.
If a book genuinely needs different overlays (different
elements highlighted), it gets its own yaml.

### OQ-03: i18n match key strategy

For multilingual UIs, the snapshot is per-language (aria names
differ). So `match: { role, name }` differs per language.

Options:

- **(a) Full per-language duplication** — separate yaml per language
- **(b) data-testid based matching** — `match: { testId: "email-input" }` is language-stable. Requires app discipline (testids on every overlay target).
- **(c) Reference-based matching** — uses snapshot `[ref=eN]` markers. Brittle (refs change across captures).

**Recommended:** (a) as default, (b) as opt-in for projects with
testid discipline. (c) not supported.

### OQ-04: Editor mode — general PNG annotator vs spec-authoring

Annot editor today is positioned as a general PNG annotation
tool. Phase 4 adds the Overlay tool which is spec-specific.

When opening an arbitrary PNG (no linked annotation yaml), the
Overlay tool is irrelevant. Options:

- **(a) Always show Overlay tool, disabled if no snapshot**
- **(b) Mode toggle in UI** — "PNG annotation" vs "Screen spec authoring"
- **(c) Auto-detect** — show Overlay tool when PNG XMP has snapshot AND linked yaml is loadable

**Recommended:** (c). Reduces UX friction for general PNG annotators while exposing the docs flow when context permits.

### OQ-05: PR-mode vs direct-push for GitHub round-trip

Phase 5's commit policy:

- **(a) PR-mode** — every edit creates a PR. Safe (review gate) but high friction (many small PRs).
- **(b) Direct push** — edits go straight to main. Fast but unsafe for production docs.
- **(c) Per-installation policy** — repo admin configures in annot-cloud dashboard.

**Recommended:** (c). Default to PR-mode for installations on
public repos; allow direct-push for solo / trusted-team repos.

### OQ-06: Embedded editor read-only mode for unauthenticated visitors

When a visitor without GitHub write access clicks Edit:

- **(a) Disabled button** — can't edit, button greyed out
- **(b) Read-only editor** — editor opens, shows annotations, but can't save
- **(c) PR suggestion flow** — fork repo + commit + PR

**Recommended:** (b) + a "Suggest changes" CTA that leads to (c).
Combines preview value with contribution path.

### OQ-07: Real-time collaboration scope

If multiple editors open the same yaml simultaneously:

- **(a) Last-write-wins** — simple, occasional data loss
- **(b) Optimistic locking** — GitHub ETags prevent overwrites, second writer sees conflict
- **(c) CRDT-based collab** — real-time merge, no conflicts ever (high implementation cost)

**Recommended:** (a) for v1, (b) for v1.1. (c) deferred
indefinitely.

### OQ-08: When does the `<Overlay>` JSX support actually get removed?

Phase 2 dual-supports `<Overlay>` JSX and the new
`<Description for>` form. When does old form get deleted?

- **(a) Hard deprecate immediately**: bump major version, drop in next major
- **(b) Soft deprecate for 2-3 release cycles**: warning at build time, drop later
- **(c) Permanent dual support**: never drop, both forms work

**Recommended:** (b). Two cycles (~3 months) of dual support
with build warnings gives external consumers time to migrate
without permanent maintenance debt.

## Discarded alternatives

### DA-01: PNG XMP carries everything (single-file vision)

**Idea**: Image + snapshot + annotations + overlay text all in
PNG XMP. MDX becomes optional, Astro reads everything from PNG.

**Fatal flaw**: Re-capture overwrites PNG. Every CI run destroys
human-edited overlay data. Sidecar `.overlays.yaml` was
proposed as mitigation, but sidecar fragility (move PNG without
yaml → data lost) is just as bad.

### DA-02: Sidecar yaml paired with PNG by filename

**Idea**: `login.png` + `login.overlays.yaml` in same directory.
Convention-based pairing.

**Fatal flaw**: Filesystem operations break the pair silently.
`mv login.png` doesn't bring the yaml. Slack-shared PNG
arrives without yaml. No build-time validation.

### DA-03: PNG snapshot + MDX inline overlays + JSX writer

**Idea**: Snapshot in PNG XMP; overlays stay as `<Overlay>` JSX
in MDX; Annot editor writes MDX directly via byte-stable JSX
rewriter.

**Fatal flaw**: MDX JSX writer is the hardest single component
in the whole architecture. remark-mdx doesn't preserve
formatting on serialization; achieving byte-stability across
insertion / deletion / reordering of JSX nodes is a multi-week
implementation that consumers (anyone writing MDX) would
notice and dislike (their carefully formatted MDX gets
reformatted by the tool).

### DA-04: Two yaml files (annotation + description)

**Idea**: Annotation in `.annotations.yaml`, description in
`.descriptions.yaml`. MDX references both.

**Fatal flaw**: Description text wants markdown formatting,
inline JSX (`<Link>`, `<code>`, `<Note>`), multi-paragraph
structure. YAML strings are awkward for all of these. MDX is
the right home for description body.

### DA-05: PNG-driven editor that writes both PNG XMP and MDX JSX

**Idea**: Editor writes annotations to PNG XMP AND writes
`<Overlay>` JSX to MDX. Bidirectional sync.

**Fatal flaw**: Combines DA-01's re-capture problem with DA-03's
JSX writer cost. Sync direction ambiguity (which is the source
of truth?) creates race conditions.

## How this relates to existing plans

- [`playwright-screenshot-fixture-relayer.md`](./playwright-screenshot-fixture-relayer.md) — Phase 0 of this roadmap. Already Draft.
- [`_done/living-product-docs.md`](./_done/living-product-docs.md) — Established the docs platform this roadmap evolves. The 3-layer model is the natural progression of that plan's authoring story.
- [`annot-cloud-roadmap.md`](./annot-cloud-roadmap.md) — Phase 3 (auth foundation) is a prerequisite for Phase 5 of this roadmap. Phase 5's GitHub App + commit logic lives in annot-cloud per [`oss-cloud-split.md`](./oss-cloud-split.md).
- [`oss-cloud-split.md`](./oss-cloud-split.md) — Phase 5's iframe boundary is the canonical example of "OSS provides the affordance, cloud provides the paid service" pattern.
- [`launch-prep.md`](./launch-prep.md) — annot-cloud launch (Phase 8 of annot-cloud-roadmap) precedes Phase 5 of this roadmap. Phase 5 is a post-launch feature.

## Timeline

Phase ordering reflects technical dependencies, not calendar:

```
Phase 0 (relayer)
    ↓
Phase 1 (PNG snapshot cache)
    ↓
Phase 2 (annotation yaml + Description JSX)
    ↓
Phase 3 (palette extension) ── independent ──→ Phase 4 (editor)
                                                    ↓
                                              Phase 5 (embedded editor)
                                                    ↑
                                              annot-cloud Phase 3 (auth)
```

Phase 3 and Phase 4 can run in parallel (Phase 3 is build-time
schema work, Phase 4 is editor UI work). Phase 5 depends on both
Phase 4 (editor capability) and annot-cloud's auth foundation.

Realistic calendar:

- Phase 0: 2026-05-22 to ~2026-06-05 (in progress)
- Phase 1: 2026-06 mid
- Phase 2: 2026-06 late to 2026-07 early
- Phase 3: 2026-07 mid (parallel with Phase 4)
- Phase 4: 2026-07 mid to 2026-08 early
- Phase 5: post-launch (annot-cloud launch is targeted 2026-Q4 per annot-cloud-roadmap), so Phase 5 starts late 2026 / early 2027

This is aggressive but plausible at 40h/wk. The dependencies are
clean; risk is mostly in Phase 4's UX iteration (visual editing
of element-anchored annotations has no clear competitor to
benchmark against).

## When to revisit this roadmap

- After Phase 0 lands — confirm hook-registry pattern matches expectations before Phase 1 builds on it.
- After Phase 2 lands — gather feedback from workflow-app users on the migration UX. The Phase 2 migration CLI is the riskiest transition; if it leaves users with broken MDX, fix before Phase 3.
- Before Phase 5 — re-evaluate the iframe-vs-inline decision (AD-08) once annot-cloud's auth is real. The current AD-08 reasoning is sound but should be re-validated.
- If a competitor ships embedded visual editing — adjust the strategic positioning section. The space is empty today but the underlying tech (postMessage + GitHub App) is well-trodden.

## References

- [`playwright-screenshot-fixture-relayer.md`](./playwright-screenshot-fixture-relayer.md)
- [`_done/living-product-docs.md`](./_done/living-product-docs.md)
- [`_done/playwright-screenshot-annot-fixture.md`](./_done/playwright-screenshot-annot-fixture.md)
- [`_done/editable-png-from-annotator.md`](./_done/editable-png-from-annotator.md)
- [`annot-cloud-roadmap.md`](./annot-cloud-roadmap.md)
- [`oss-cloud-split.md`](./oss-cloud-split.md)
- [`launch-prep.md`](./launch-prep.md)
- [`github-integration.md`](./github-integration.md) — individual-user GitHubStore (consumed by Phase 5)
- `PRODUCT_DIRECTION.md` — strategic north star (third growth vector: living product docs)
