# Plans

> Design documents for queued and in-progress work. Each plan is a
> living doc — update the status header when state changes, and move
> to `_done/` when landed so the active list stays scannable.

Plans sit between `PRODUCT_DIRECTION.md` (the "why") and the code
(the "how it ended up"). They capture the "how we intend to do it"
before implementation, which is invaluable for:

- Landing big refactors cohesively (no half-merged states).
- Sharing intent when multiple sessions / contributors iterate.
- Letting Claude Code read the plan and resume work across context
  resets.

## Active plans

Plans being discussed (`Draft`), waiting for an implementation
window (`Queued`), or actively shipping (`In progress`).

| Plan | Status | Summary |
|------|--------|---------|
| [`card-pptx-templates.md`](./card-pptx-templates.md) | Partially landed | Rework the PPTX exporter from hand-rolled per-slide OOXML to a proper slide master + slide layouts + slide content tier. Built-in `.pptx` templates authored in PowerPoint (one per HTML theme) live in `packages/render/src/pptx/templates/`; placeholders named `Annot.StepBadge` / `Annot.StepTitle` / `Annot.StepImage` etc. drive content emission. Adds custom-template upload (validation + `StorageProvider` persistence). Six phases (master + layout refactor / built-in registry + ZIP read via `fflate` / Appearance dialog picker / custom upload / placeholder-aware emission / authoring docs + archival). Phase 1's PPTX golden regenerates — PR description must call out the new bytes. Adds `meta.appearance.pptxTemplate`; pairs with [`card-document-themes.md`](./card-document-themes.md). |
| [`plugin-host-extraction.md`](./plugin-host-extraction.md) | Draft | Follow-up to [`_done/host-convergence.md`](./_done/host-convergence.md) Phase 4. After Phase 4 moves the plugin-host **types** into `@ingcreators/annot-host-ui` (so the gallery can import them post-Phase-2), this plan moves the `PluginHost` class itself. The class is mostly host-agnostic already; one PWA-internal dependency (`BUILT_IN_STORAGE_MODES` from `web/src/storage/bridge.ts`) gets injected as a constructor option. Three phases: (1) copy + inject + add subpath export + leave a re-export shim in `annot-web`; (2) PWA migrates off the shim; (3) docs + archival. **Trigger to flip Draft → Queued: Desktop or VSCode signals an intent to load plugins**; until then the plan stays warm — the design is captured, dependencies are audited, but the actual code move waits for a concrete consumer (no point reorganising for its own sake). Two plausible trigger scenarios documented inline (`annot-cloud` "team library" plugin on Desktop; "Link to repository" plugin on VSCode). |
| [`design-system-foundations.md`](./design-system-foundations.md) | Partially landed | Phase 1 ([#286](https://github.com/ingcreators/annot/pull/286)) shipped the five-section semantic taxonomy, `localStorage` theme persistence, and the `setThemeOverrides` / `clearThemeOverrides` / `getThemeOverrides` API in `@ingcreators/annot-editor`. Phase 2 ([#287](https://github.com/ingcreators/annot/pull/287)) hard-renamed every CSS variable to the `--annot-*` namespace (one PR instead of the planned three with aliases — pre-release, no plugin consumers). Self-hosted users can already brand the UI today via `:root` overrides, a startup-script `setThemeOverrides({...})` call (with persistence), or DevTools tinkering. Phases 3 (Settings UI), 4 (plugin theme presets), and 5 (`prefers-color-scheme` follow mode) are deferred — resume when there's a concrete user-facing trigger; each is self-contained in the plan. |
| [`focus-mask-tool.md`](./focus-mask-tool.md) | Draft | New top-level annotation primitive: dim / light overlay around a rect / rounded / ellipse cutout. Closes the last gap between the in-house manual-creation PowerPoint template (`annot_designer_annotation_template.pptx`) and the Annot toolbox. SVG uses one `<g data-type="focus-mask">` wrapping a `<path fill-rule="evenodd">`; OOXML uses a single `<a:custGeom>` with two subpaths in one `<a:path>` for cross-version PowerPoint reliability. Six phases, `data-annot-version` bump 1 → 2. Phase 5 either tunes only focusMask defaults to manual-template values, or splits a sibling `preset-themes.md` plan covering the broader role-coloured palette. |
| [`pre-release-final-pieces.md`](./pre-release-final-pieces.md) | Draft | Two independent OSS-readiness gaps surfaced by the 2026-04-27 friction audit ([#227](https://github.com/ingcreators/annot/pull/227)–[#233](https://github.com/ingcreators/annot/pull/233)) and grouped here because both get harder to add later: (1) `.github/` templates — issue forms (bug + feature), PR template mirroring the `Verified:` checklist, `CODEOWNERS`, `FUNDING.yml`; (2) Changesets bootstrap — `@changesets/cli` + `.changeset/config.json` + workflow doc, with no automation and no `private: false` flips. Three independent PRs (1a / 1b / 2). Root `CHANGELOG.md` preserved. |
| [`google-drive-integration.md`](./google-drive-integration.md) | Draft | Rework the Drive backend onto the non-sensitive `drive.file` scope + Workspace Marketplace + Drive UI Integration so Annot can ship publicly without a restricted-scope CASA audit. |
| [`oss-cloud-split.md`](./oss-cloud-split.md) | Draft | Forward-looking strategy for keeping Annot OSS while developing paid features in a separate private `annot-cloud` repo. Guardrails apply from today; concrete phases trigger on "first paid feature" and "company incorporation". |
| [`github-integration.md`](./github-integration.md) | Draft | Individual-user `GitHubStore` — device-flow auth, pick repo + branch + base path, commits as save. Drive-integration-equivalent scope. PR automation / Check Runs / org admin live in `annot-cloud`, not here. Phases 1–3 landed (#36–#38); Phase 4 polish in progress. |
| [`web-capture-redesign.md`](./web-capture-redesign.md) | Queued | Replace the parallel `Capture Screen` + `Timed Capture...` New-menu entries with a single `Capture Screen...` dialog → three modes (Auto Capture / Capture Once / Capture Area). New `/capture` route hosts a `<annot-capture-workspace>` with live `<video>` preview + candidate panel. Auto Capture is change-detection-driven (downsampled-canvas pixel-delta + cursor-only ignore + state-machine stable-wait) and pushes to an in-memory `CandidateStore` users triage via Accept / Delete / Edit. Capture Once continues to save directly. Six PRs: Phase 0 plan / Phase 1 dialog scaffold + Capture Once / Phase 2 `/capture` route + workspace shell / Phase 3 candidate model scaffold / Phase 4 Auto Capture engine / Phase 5 retire Timed Capture. Capture Area (selection overlay + coordinate mapping), high-DPI warnings, save-size presets, advanced settings panel, settings persistence, IDB / OPFS candidate storage, **and adoption of the existing extension `encodeCapture()` smart PNG-8 pipeline** (libimagequant quantisation with JPEG fallback for photo-heavy frames; no WebP — that framing of spec §14 is dropped) are all explicitly deferred to future PRs. No `StorageProvider` / `ImageRecord` / SVG schema changes. PWA only — VSCode / Desktop / Extension hosts unchanged. |

## Recently landed plans

Most recent entries in [`_done/`](./_done/), newest first. The
historic full list is the directory itself; this table is the
"first-page" view a reader scanning the repo will see.

| Plan | Landed | Summary |
|------|--------|---------|
| [`_done/card-document-themes.md`](./_done/card-document-themes.md) | 2026-05-12 | Split `injectDocumentStyles` into a fixed structural layer (grid, `[data-annot-block]` selectors, counters, print rules) + a swappable theme layer (CSS custom property values, badge styling, typography scale). Built-in theme registry — `modern-light` (default) / `modern-dark` / `minimal` / `editorial` / `playful` — plus a `customCss` escape hatch with sanitisation (no `@import`, no external `url()`, 8 KB cap). Six phases each landed as a standalone PR: (1) [#632](https://github.com/ingcreators/annot/pull/632) structural / theme CSS split (`Theme` type, `legacy.ts`, `THEMES` registry, byte-equivalent refactor); (2) [#633](https://github.com/ingcreators/annot/pull/633) 3 new themes + `meta.appearance.template` precedence over legacy `meta.theme` + 5 Storybook stories; (3) [#634](https://github.com/ingcreators/annot/pull/634) Doc Settings Appearance picker (radio cards with descriptions + Legacy fallback + cross-toggle with the legacy Theme dropdown); (4) [#635](https://github.com/ingcreators/annot/pull/635) font family override via postlude CSS — three text inputs (sans / serif / mono) accepting logical tokens or raw CSS family stacks; (5) [#636](https://github.com/ingcreators/annot/pull/636) custom CSS escape hatch + `sanitiseCustomCss` Tier A helper + Advanced expander in the dialog + sanitiser warnings surfaced via `showSaveError`; (6) #TBD plugin-API themes doc + `meta.appearance` reference in `docs/annot-html-format.md` + CLAUDE.md guardrail #11 + plan archival. Schema delta purely additive — `data-annot-doc-version` stayed at 1. Existing documents (no `meta.appearance` set) continue to use the legacy `meta.theme` mapping byte-identically — proven by the 6-fixture round-trip test re-asserted at every phase. Paired with [`card-pptx-templates.md`](./card-pptx-templates.md) — each HTML theme will gain a matching PPTX template so a single "Appearance" choice carries both surfaces (still pending). |
| [`_done/card-step-auto-numbering.md`](./_done/card-step-auto-numbering.md) | 2026-05-12 | Replaced the pre-filled `"Step N"` / `"Image N"` title strings in card documents with a CSS-counter-driven Scribe-style numbered badge on `[data-annot-block="step"]::before`. Reorder via the existing drag-handle mechanism updates the numbers automatically. Five phases each landed as a standalone PR: (1) [#627](https://github.com/ingcreators/annot/pull/627) data layer (`NumberingMeta.steps` / `stepLabel`); (2) [#628](https://github.com/ingcreators/annot/pull/628) CSS counter + badge styling in `inject-styles.ts` + 11 new Storybook stories; (3) [#629](https://github.com/ingcreators/annot/pull/629) Doc Settings dialog wiring (checkbox + label-format dropdown with 4 presets + Custom escape hatch); (4) [#630](https://github.com/ingcreators/annot/pull/630) drop title pre-fill in `buildStepTitle` + remove `CardDocumentNumbering` type + default new card docs to `meta.numbering.steps = true`; (5) #TBD PPTX badge shape emission (`<p:sp>` with pill-shaped `prstGeom`, accent-colour fill + soft shadow, translucent-dark overlay for image-fill layout) + format docs + archival. Schema delta purely additive — `data-annot-doc-version` stayed at 1. No migration of pre-existing card documents per user direction. The scope-deferred "bleed badge outside the card boundary" path was traded for an inside-the-card placement (Phase 2's scope-deviation note), avoiding a DOM restructure / version bump while keeping the Scribe-style visual. Foundation for [`card-document-themes.md`](./card-document-themes.md) and [`card-pptx-templates.md`](./card-pptx-templates.md) — both reference the badge as a themable / templatable surface. |
| [`_done/card-procedure-template.md`](./_done/card-procedure-template.md) | 2026-05-12 | Card-style step documents on top of the `.annot.html` v1 format ([`_done/annot-html-document.md`](./_done/annot-html-document.md)). One new block kind (`step`) carrying image + title + body + per-card layout (`image-top` / `image-bottom` / `image-left` / `image-right` / `image-fill`), a doc-level `cardLayout.columns` grid setting (1 / 2 / 3 / auto), a gallery-selection-order-driven generator ("Create card document from selection"), built-in `card-procedure` starter template, and PPTX export widened to emit one slide per step. Twelve phases (0 – 7d) plus a UX-polish wrap-up: image viewport (Phase 7d — pan / zoom / save-view via `attachStepImageViewport`); image-less step blocks (7a); URL link chip (7b); document header + PPTX cover slide (7c). Wrap-up PRs [#610](https://github.com/ingcreators/annot/pull/610)–[#624](https://github.com/ingcreators/annot/pull/624) closed the grey strip on annotated cards (id="svg-root" leakage stripped on export + `margin: 0` doc-side guard + revert of the `position:absolute` slot fix that broke card grid sizing), switched the viewport's default rect to contain semantics so portrait sources show in entirety inside the 16:9 frame, gave `<annot-doc-shell>` `display: block` so full-width docs claim their space, hid between-card insert-bars in multi-column mode so the grid actually packs horizontally, zeroed PPTX text-shape `<a:bodyPr>` insets to match the SVG layout (no double margin in PowerPoint), and finally moved the per-card zoom toolbar to hover-reveal + gated wheel zoom behind Ctrl / Cmd. Additive throughout — `data-annot-doc-version` stayed at 1; no migration / forward-compat work was needed. |
| [`_done/annot-html-document.md`](./_done/annot-html-document.md) | 2026-05-12 | v1 of the `.annot.html` file format — a single HTML document that reads as a normal manual / runbook in any browser AND opens in Annot as a fully editable block document, with inline `.annot.svg` per image block. Fourteen phases (0 – 13c) shipped via PRs #539 – #568: spec freeze + golden fixtures (0); Tier A `@ingcreators/annot-doc` parser / serialiser / round-trip + structural-clone helper (1); self-contained styling — no external CSS / JS / fonts so files commit to repos and render in GitHub previews (2); read-only `<annot-doc-shell>` (3); block editing + slash menu (4); image block + EditorShell modal + capture insertion (5); PWA route + BrowserStore + gallery wiring (6); multi-backend opt-in for Device / Desktop / Drive / GitHub via optional `StorageWithDocuments` capability so existing `StorageProvider` consumers continue to work unchanged (7); templates mechanism — clone / save-as / picker / "From Template…" (8); built-in starter templates (9); VSCode custom editor + `Annot: New document` (10); multi-slide PPTX export (11); auto-numbering for headings + figures (13a); cross-reference resolver helper (13b); plugin docs for `StorageWithDocuments` (13c). Phase 12 (Markdown export) deferred to demand. Companion UX polish landed under [`_done/annot-html-document-ux-polish.md`](./_done/annot-html-document-ux-polish.md); the card-style extension under [`_done/card-procedure-template.md`](./_done/card-procedure-template.md). |
| [`_done/annot-html-document-ux-polish.md`](./_done/annot-html-document-ux-polish.md) | 2026-05-10 | 13 phases of UX polish on the `.annot.html` document feature, taking it from "functional v1" to "product-ready". One PR per phase ([#576](https://github.com/ingcreators/annot/pull/576)–[#588](https://github.com/ingcreators/annot/pull/588)) each independently revertable. Header strip with title / save status / mode toggle / undo-redo / +Image / overflow menu (1); persistent insert-bars + drag handle (2); inline selection format toolbar with B/I/U + 10-item block-kind dropdown (3); empty-state onboarding with 4 cards + "Use a template" → existing picker (4); contentEditable extension to list items / quote / callout / figcaption with Enter split + exit-on-empty-trailing (5); image flow polish with drop-zone overlay + multi-paste / multi-drop sequencing + always-visible click-to-edit + modal "Editing image N of M" header + dirty tracking + Esc-to-cancel guard (6); HTML5 drag-and-drop block reorder (7); Ctrl+/ Ctrl+Enter Ctrl+Shift+1/2/3/7/8/> keyboard shortcuts surfaced in the `?` keyboard-help modal (8); mobile sweep — TOC hamburger toggle, ≥44px touch targets, modal full-screen ≤600 px (9); lazy SVG materialisation via IntersectionObserver with viewBox-derived placeholder aspect-ratio (10); document settings dialog (title / lang / author / theme / maxWidth) flowing through DocumentHistory (11); Copy-document-HTML clipboard + Print + save-status retry button on error (12); a11y / reduced-motion sweep + plan archival (13, this entry). Three deferrals tracked in the plan: long-press Pointer-Events drag for touch reorder (Phase 7), per-block diff history (Phase 10), Save-as to a different store / path (Phase 12). No SVG / HTML format change, no `data-annot-doc-version` bump, no `StorageProvider` change, `@ingcreators/annot-doc` Tier A surface frozen throughout. |
| [`_done/destructive-crop-bake.md`](./_done/destructive-crop-bake.md) | 2026-05-09 | Fixed the half-finished `CropTool` (visual feedback was broken — the dimming overlay covered the whole image with no punch-out — and the crop wasn't persisted across save / reload because `History.save()` only snapshots `annotations.innerHTML`). Single-PR rewrite: visual punch-out via a single `<path fill-rule="evenodd">` (outer viewBox + inner crop subpath); `<foreignObject>`-hosted Apply / Cancel buttons anchored to the crop rect; destructive bake pipeline mirroring `applyAllRedactions`: new Tier C-render `cropBitmap` helper, new Tier B `bakeAnnotationsTranslate` (handles non-line shapes via `bakeTranslate` + lines / arrows via the new `bakeLineTranslate`), `EditorShell.applyCrop(x, y, w, h)` orchestration that swaps the bitmap, shifts every annotation by `(-x, -y)`, resets the SVG viewBox, and persists `originalDataUrl` + `width` + `height` + `annotationsSvg` in one `storage.updateImage` call (`ImageRecordUpdate` extended with optional `width` / `height`; google-drive + github stores propagate them through the XMP rebuild). Confirmation dialog ("Crop image?" / destructive style) wired in all three hosts (PWA / VSCode / Desktop) via the new `Toolbar.options.applyCrop` → `ToolFactoryDeps.applyCrop` → `CropTool.onCropConfirmed` chain; legacy session-only viewBox fallback preserved for hosts that haven't wired the gate. After landing, all three hosts surface the crop action; saved files persist the cropped dimensions on reload. No `data-annot-version` bump (the on-disk SVG just shrinks). |
| [`_done/redact-burn-into-image.md`](./_done/redact-burn-into-image.md) | 2026-05-08 | Closed the privacy gap in the overlay-only redact tool. New "Apply redactions to image" action bakes every redaction into the base bitmap and removes the redact elements from the SVG. Seven phases each landed as a standalone PR: (1) [#505](https://github.com/ingcreators/annot/pull/505) Tier C-render `burnRedactionsIntoBitmap` helper in `@ingcreators/annot-render` — pure data-driven `(base, redactEls) → Blob`, dispatches per `data-redact-style` (solid → `fillRect`, mosaic / blur → embedded-PNG `drawImage`); (2) [#506](https://github.com/ingcreators/annot/pull/506) `EditorShell.applyAllRedactions(): Promise<{ count: number }>` orchestration — snapshots redacts, drives the renderer, swaps `imageEl.href` + in-memory `originalDataUrl`, removes redacts, saves history snapshot, emits `dirty`; (3) [#507](https://github.com/ingcreators/annot/pull/507) PWA right-panel `<annot-apply-redactions-button>` Lit component opening a destructive-style `showConfirmDialog` and dispatching an `applied` CustomEvent on confirm; (4) [#508](https://github.com/ingcreators/annot/pull/508) renderer extended to honour `data-rot` / `data-flip-h` / `data-flip-v` via `M = T(c) * R(θ) * S(sx,sy) * T(-c)` applied to the canvas context (PWA gets rotated-redaction parity, no original pixels leak at the rotated bounds); (5) [#509](https://github.com/ingcreators/annot/pull/509) PWA toast wiring — `EditorSession` listens for `applied` and calls `showInfo("N redaction(s) applied to image. Save to make permanent.")` via the existing `<annot-error-bar>` info-severity surface; (6) [#510](https://github.com/ingcreators/annot/pull/510) VSCode + Desktop parity — both hosts wire `panel.applyAllRedactions = () => shell.applyAllRedactions()` plus `panel.refreshRedactCount()` on initial mount + every `dirty`. After landing, all three hosts surface the burn action; saved files no longer carry recoverable original pixels under their redactions. No `data-annot-version` bump (saved files just lose their redact elements after burn-in); no `StorageProvider` change (the host's existing save pipeline persists the new bytes). |
| [`_done/desktop-browser-mode.md`](./_done/desktop-browser-mode.md) | 2026-05-07 | Full extension parity for the Electron Browse window via a new shared `@ingcreators/annot-capture` package extracted from `packages/extension`. Nine phase-PRs landed in one session: 1A ([#478](https://github.com/ingcreators/annot/pull/478)) package scaffold + chrome-free moves, 1B ([#479](https://github.com/ingcreators/annot/pull/479)) orchestrator extraction + `ChromeCaptureHost`, 2 ([#480](https://github.com/ingcreators/annot/pull/480)) DPR returned from `host.captureViewport()`, 3 ([#481](https://github.com/ingcreators/annot/pull/481)) Browse window's visible-mode wired through the shared orchestrator, 4A ([#482](https://github.com/ingcreators/annot/pull/482)) `<webview>` content preload + Area / Full-Page / Per-Page modes, 4B ([#483](https://github.com/ingcreators/annot/pull/483)) Click / Hotkey / right-click context-menu modes, 5 ([#484](https://github.com/ingcreators/annot/pull/484)) multi-tab Browse window + `window.open` routing, 5B ([#487](https://github.com/ingcreators/annot/pull/487)) `window.opener` preservation for OAuth popups, 6 ([#485](https://github.com/ingcreators/annot/pull/485)) capture-settings UI + persistence as `<annot-capture-settings>` in `@ingcreators/annot-host-ui` (extension's `options.html` ported to the same component in [#486](https://github.com/ingcreators/annot/pull/486) as the immediate consumer). After landing, Desktop has all six capture modes (visible / area / full-page / per-page / click / hotkey) at extension parity over a Chromium-uniform path; the original per-OS WebView2 / WKWebView / WebKitGTK Phases 3–6 outlined in the plan's first revision are gone, collapsed onto Electron's uniform Chromium renderer. |
| [`_done/host-convergence.md`](./_done/host-convergence.md) | 2026-05-07 | Closed the cross-host structural unevenness left over from the Apr–May 2026 plan series. Desktop adopted `EditorShell` + the PWA-equivalent chrome ([#470](https://github.com/ingcreators/annot/pull/470), Phase 1). The gallery surface (`<annot-file-manager-shell>` / `<annot-sidebar>` / `<annot-gallery-page>` / `<annot-context-menu>` / `FileManager`) plus thumbnail-manager + dialog + plugin-host structural types + capture predicates + `BUILT_IN_STORAGE_MODES` + `file-manager.css` moved into `@ingcreators/annot-host-ui` ([#471](https://github.com/ingcreators/annot/pull/471), Phase 2 — Decision 4 folded Phase 4's narrowed structural-types scope in here). The orchestrator triple lifted into host-ui per-orchestrator PRs with constructor-injected host element + deps callbacks: StatusHost ([#472](https://github.com/ingcreators/annot/pull/472)), SavePipeline ([#473](https://github.com/ingcreators/annot/pull/473)) — Desktop autosave migrated off the Phase 1 stopgap, HeaderHost ([#474](https://github.com/ingcreators/annot/pull/474)) — Desktop migrated off the Phase 1 inline header build. Cosmetic rename followed: `@ingcreators/annot-editor-shell` → `@ingcreators/annot-host-ui` ([#475](https://github.com/ingcreators/annot/pull/475)) — the package's post-Phase-3 scope is now gallery + dialog + thumbnail-manager + plugin-host types + orchestrators, not just the editor surface. The companion `annot-web` → `annot-pwa` rename in the plan's Forward-looking notes was declined (`annot-web` keeps its name). Phase 4 folded into Phase 2; Phase 5 (capture extraction) deferred to [`desktop-browser-mode.md`](./desktop-browser-mode.md) Phases 1–2. The runtime `PluginHost` class itself is tracked in [`plugin-host-extraction.md`](./plugin-host-extraction.md) (Draft, gated on Desktop / VSCode signalling intent to load plugins). |
| [`_done/desktop-electron-migration.md`](./_done/desktop-electron-migration.md) | 2026-05-06 | Replaced the entire `packages/desktop/src-tauri` Rust crate with a Node-side Electron main process. Ten phases each landed as a standalone PR ([#446](https://github.com/ingcreators/annot/pull/446)–[#455](https://github.com/ingcreators/annot/pull/455)): scaffold side-by-side (0), fs-primitives IPC over `DesktopStore` (1; collapsed from "port SQLite + 8 IPC commands" to a small fs-IPC module after [`_done/desktop-storage-provider-migration.md`](./_done/desktop-storage-provider-migration.md) landed first), settings + XMP + http-server + window controls (2), screen capture cross-platform from day one via Chromium `desktopCapturer` (3), Office clipboard via an N-API addon built on `windows-latest` (4), default-to-Electron cutover + `tauri-bridge.ts` → `desktop-bridge.ts` rename (5), Browse window Electron-native rewrite supersedes the original `desktop-browser-mode.md` Phases 3–6 (6), macOS notarization recipe (7), Linux packaging polish (8), Tauri sources + `@tauri-apps/*` deps removal (9, [#455](https://github.com/ingcreators/annot/pull/455)). Per-user install + auto-update without admin via `electron-builder` (`%LOCALAPPDATA%\Programs\Annot` on Windows). ~10× installer-size jump (10 MB → 80–120 MB) accepted as the cross-platform-parity trade. CLAUDE.md guardrail #5 wording updated in Phase 5. |
| [`_done/desktop-storage-provider-migration.md`](./_done/desktop-storage-provider-migration.md) | 2026-05-06 | Replaced the desktop host's bespoke SQLite-backed gallery (`projects` + `images` tables, renderer-side `gallery.ts` / `project-manager.ts`, 6 IPC commands) with `DesktopStore` — a `StorageProvider` implementation that mirrors `DeviceStore`'s file-system + per-file-XMP model. The desktop now mounts the unified `<annot-file-manager-shell>` + `FileManager` from `@ingcreators/annot-web/gallery/...` against a filesystem library at `<userData>/library/`. Six phases each landed as a standalone PR: (0) [#440](https://github.com/ingcreators/annot/pull/440) feature audit + design freeze (companion deliverable [`_done/desktop-storage-provider-audit.md`](./_done/desktop-storage-provider-audit.md) folded five recommendations back into the parent plan); (1) [#441](https://github.com/ingcreators/annot/pull/441) `DesktopStore` core + contract tests against an in-memory `DesktopFs` mock — adds `./storage/*` subpath export to `annot-web` so the desktop can reuse `buildEditableImageBlob` + `runStorageContract` without a local copy; (2) [#442](https://github.com/ingcreators/annot/pull/442) bridge wiring + parallel mount under `localStorage.annotDesktopStorageMode === "fs"` flag — adds a `desktop` chip to `BUILTIN_CHIP_DESCRIPTORS` + the `desktop_windows` Material Symbol; (3) [#443](https://github.com/ingcreators/annot/pull/443) capture pipelines (full-screen / window / region / extension HTTP push / Native Messaging handoff / paste) all route through `persistViaDesktopStore` → `DesktopStore.saveImage`; (4) [#444](https://github.com/ingcreators/annot/pull/444) default-flip + one-time legacy-data notice (literal `<portable_dir>/data/` path + `plugin-shell.open` reveal affordance, gated on the legacy `annot.db` existing) + a Settings dialog rollback toggle; (5) #TBD cleanup — deleted `gallery.ts` / `project-manager.ts` / `db.rs` / `commands/{capture,images,projects}.rs` / the `Database` registration in `lib.rs` / `rusqlite` from `Cargo.toml` / 9 SQLite-tied exports from `tauri-bridge.ts` / the rollback toggle from the Settings dialog (the legacy codepath is gone; the Phase 4 toast remains as the only mention of `<portable_dir>/data/`). CLAUDE.md monorepo-layout `desktop` row updated with the new framing. **No data migration** by design — pre-release install had no production captures; legacy `data/` directory left untouched on disk for the user to back up or remove. Library at `<userData>/library/` (OS conventions: `%APPDATA%/Annot` / `~/Library/Application Support/Annot` / `~/.config/Annot`) so admin rights aren't needed and auto-update preserves the library by definition. Lands **before** `desktop-electron-migration.md` so its Phase 1 ("DB + projects + images IPC parity") collapses to "fs primitives only". |
| [`_done/editor-session-shell-switchover.md`](./_done/editor-session-shell-switchover.md) | 2026-05-04 | Retired the deferred Step 4 from the [`vscode-extension-host.md`](./_done/vscode-extension-host.md) series — the PWA's `EditorSession.setupEditor` now boots through `EditorShell.mountFromRecord` instead of constructing `CanvasManager` / `History` / `SelectionManager` directly. Five phases each landed as a standalone PR: (1) [#411](https://github.com/ingcreators/annot/pull/411) CSS dual-selector for `#svg-root` + `[data-annot-shell-root]` so attribute-keyed and id-keyed SVG roots match the same rules; (2) [#412](https://github.com/ingcreators/annot/pull/412) optional `svgRoot?: SVGSVGElement` knob on `EditorShellHost` so a host can adopt an existing pre-baked SVG (PWA / Tauri-desktop) instead of letting the shell create an anonymous one; (3) [#413](https://github.com/ingcreators/annot/pull/413) moved `restoreAnnotations` into the shell as part of `mountFromRecord` and replaced the web-side helper with a shim; (4) #TBD `EditorSession` switchover (the headline change — synthesizes a sparse `ImageRecord` from the per-call `(dataUrl, width, height, annotations)` + currently-open record and delegates to `shell.mountFromRecord`; selection-change + dirty handlers wired via `shell.on(...)`); (5) #TBD cleanup — shim deletion, CLAUDE.md guardrail #10 amend, plan archival, vscode-extension-host carry-over struck through. No SVG schema bump, no `StorageProvider` change, no `PageMetadata` change. Unblocks `playwright-integration.md` (TBD) and a future "desktop hosts the shell directly" plan. |
| [`_done/vscode-extension-host.md`](./_done/vscode-extension-host.md) | 2026-05-04 | Extracted the editor surface out of the PWA shell into a new host-neutral `@ingcreators/annot-host-ui` package and stood up `@ingcreators/annot-vscode` as the second consumer (PWA being the first). Shell owns the per-image lifecycle (`CanvasManager` + `History` + `SelectionManager` mounted into a host-supplied container); hosts wire toolbar / drawer / right-panel / save pipelines on top via `getCanvas/getHistory/getSelection` + the `dirty/saved/error/selection-change` event bus. PRs [#395](https://github.com/ingcreators/annot/pull/395) (scaffold), [#396](https://github.com/ingcreators/annot/pull/396) (keyboard-help), [#397](https://github.com/ingcreators/annot/pull/397) (drawer + tag-editor + lit/UISection/icon infra), [#398](https://github.com/ingcreators/annot/pull/398) (toolbar cluster — Phase 2c/2d order swap rationale per its description), [#399](https://github.com/ingcreators/annot/pull/399) (right-panel cluster), [#400](https://github.com/ingcreators/annot/pull/400) (scratchpad UI + ScratchpadStoreLike contract), [#401](https://github.com/ingcreators/annot/pull/401) (EditorShell body + 8 tests), [#402](https://github.com/ingcreators/annot/pull/402) (`@ingcreators/annot-vscode` skeleton + `VSCodeStore` over `vscode.workspace.fs` + `*.annot.{svg,png,jpeg,jpg}` custom-editor contribution), [#403](https://github.com/ingcreators/annot/pull/403) (UX polish — status bar, theme bridging, command palette entries, marketplace metadata), and #404 (Phase 6 archival + CLAUDE.md guardrail #10 + CI invariant). The PWA `EditorSession` switch-over (still constructs `CanvasManager` directly today) is queued as a follow-up under the `editor-session-shell-switchover` ticket. Phase 5 deferrals tracked in the same plan series: full webview ↔ extension `StorageProvider` proxy, XMP round-trip for raster files, full save-as / export commands. |
| [`_done/multilingual-fonts-os-stack.md`](./_done/multilingual-fonts-os-stack.md) | 2026-05-03 | OS fonts only (no embedding) for multilingual content. Three logical tokens (`Annot Sans` / `Annot Serif` / `Annot Mono`) the editor writes into `data-font-family`; the host's `fonts.css` plus `injectLogicalFontStyles` in `exportSVGString` map each to an OS-aware family stack interleaving Latin / CJK / Arabic / Indic / Thai. PPTX export expands each token to a `<a:latin>` + `<a:ea>` + `<a:cs>` triple so PowerPoint's per-script fallback applies symmetrically. PNG raster awaits `document.fonts.ready`. PRs [#388](https://github.com/ingcreators/annot/pull/388) (Tier-A registry + 17 tests), [#389](https://github.com/ingcreators/annot/pull/389) (host `fonts.css` injection), [#390](https://github.com/ingcreators/annot/pull/390) (editor pickers + storage normalisation via `coerceToLogicalFamily`), [#391](https://github.com/ingcreators/annot/pull/391) (PPTX 3-typeface emit + 3 tests), [#392](https://github.com/ingcreators/annot/pull/392) (PNG self-contained SVG + font-ready guard), and #393 (CLAUDE.md guardrail #8 + plan archival). After landing, the same Annot document renders with native OS fonts on Windows / macOS / Linux for English / 日本語 / العربية / देवनागरी / ไทย etc., and an exported PPTX opens with the right per-script typeface in PowerPoint without any embedded font payload. Pre-release — legacy raw font-family strings (`"sans-serif"`, `"system-ui, ..."`) coerce to `Annot Sans` on next save. |
| [`_done/move-bakes-coordinates.md`](./_done/move-bakes-coordinates.md) | 2026-05-03 | Unified move semantics: every drag bakes position into the children's geometry attrs; `transform` reserved for rotation / flip only. Removes the asymmetry where `<g data-type="shape">` (Sticky / Callout / Textbox), `<path>` (Freehand / Redact-path), `<g data-marker>` (Counter), and `<g data-type="group">` moved via `data-tx` / `data-ty` + `transform="translate(...)"` while geometry-positioned shapes (rect / ellipse / text / line / arrow) baked. PRs [#380](https://github.com/ingcreators/annot/pull/380) (`translatePathD` Tier-A helper + 26 tests), [#381](https://github.com/ingcreators/annot/pull/381) (per-shape `bakeTranslate` helpers + 22 tests), [#382](https://github.com/ingcreators/annot/pull/382) (`#moveElement` switchover, leftover `data-tx`/`data-ty` from earlier rotated drags get folded into the bake), [#383](https://github.com/ingcreators/annot/pull/383) (Phase 4 audit doc — `#worldBBox` stays everywhere because every call site touches potentially-rotated shapes), and #384 (Phase 5 cleanup — CLAUDE.md guardrail #7, plan archived). After landing, `getBBox()` returns the visual bbox for every unrotated shape; rotated-shape callers continue to use `getWorldBBox` to compose the matrix. The marquee-selects-old-position bug ([#378](https://github.com/ingcreators/annot/pull/378)) is now fixed at the source — geometry == world for unrotated shapes. Pre-release; no backward-compat shims for documents previously saved with `data-tx` / `data-ty` (the next move bakes them in). |
| [`_done/rich-text-and-shape-text.md`](./_done/rich-text-and-shape-text.md) | 2026-04-29 | Unified text handling: every text-bearing element shares one `<g data-type="shape" data-shape-kind="...">` skeleton; per-character formatting (bold / italic / underline / mixed font / size / color) round-trips via `TextRun[]` carrier on `AnnotationShape` and per-`<tspan>` SVG attrs. Six phases shipped: [#326](https://github.com/ingcreators/annot/pull/326) (skeleton + reader/writer + spec doc), [#327](https://github.com/ingcreators/annot/pull/327) (contentEditable ↔ TextRun mapper + Ctrl+B/I/U + floating mini-toolbar), [#328](https://github.com/ingcreators/annot/pull/328) (Pattern A — dblclick a bare rect to add text; lazy promotion + rollback on cancel), [#329](https://github.com/ingcreators/annot/pull/329) (textBold / textItalic / textUnderline PropertyPanel toggles), [#330](https://github.com/ingcreators/annot/pull/330) (OOXML rich-text goldens), and #331 (cleanup + archival). Schema stayed at v1 (pre-release — new attrs absorbed in place, no v2 bump); no backward compat with pre-Phase-1 dumps. |
| [`_done/unified-thumbnail-cache.md`](./_done/unified-thumbnail-cache.md) | 2026-04-29 | Lifted the per-store thumbnail lifecycle (`#ensureThumbnail` / dedup map / `annot-thumbnail-ready` dispatch / quota) into a host-owned `ThumbnailManager` + `IndexedDBThumbnailCache`. Six phases shipped: (1) [#317](https://github.com/ingcreators/annot/pull/317) cache abstract + IDB impl; (2) [#318](https://github.com/ingcreators/annot/pull/318) `ThumbnailManager` + `StorageWithThumbnailCache` capability interface; (3) [#319](https://github.com/ingcreators/annot/pull/319) Drive integration; (4) [#320](https://github.com/ingcreators/annot/pull/320) GitHub integration + `GitHubBlobCache` slimming (5 maps → 2); (5) [#321](https://github.com/ingcreators/annot/pull/321) Browser + Device integration + `ImageRecordUpdate.thumbnailDataUrl` field removal; (6) #322 plugin-API doc + `plugin:<mode>:` namespace enforcement in `bridge.ts`. After landing, every `StorageProvider` (built-in + plugin) gets cross-session thumbnail persistence + LRU eviction + in-flight dedup + event dispatch for free; the Drive/GitHub `#ensureThumbnail` duplication is gone, plugin authors only implement 3 short methods. Plugin docs at [`docs/plugin-api/storage.md`](../plugin-api/storage.md). |
| [`_done/svg-icons-and-plugin-icon-spec.md`](./_done/svg-icons-and-plugin-icon-spec.md) | 2026-04-28 | Unified the icon system onto inline SVG (dropped the bundled Material Symbols TTF — 977 KB × 2) and exposed the same icon API to plugins as `IconSpec` (`builtin` / `svg` / `url` discriminated union). 8 phases shipped in a single session ([#298](https://github.com/ingcreators/annot/pull/298)–[#306](https://github.com/ingcreators/annot/pull/306) plus the Phase 7 archive PR). Plugin-author surface lives at [`docs/plugin-api/icons.md`](../plugin-api/icons.md). The Tier-A `IconSpec` + Tier-B registry + sanitiser + renderer + `<annot-icon>` element + first-party migration (~131 sites across 41 files; 94 glyphs in the registry — 84 Material + 5 aliases + 5 hand-rolled groups) + plugin-API rename (`StorageRegistration.icon` / `SidebarTab.icon` / `ExternalLink.icon`) + font deletion all landed. Bundle delta: web precache 1820 KiB → 872 KiB. Deviation: Phases 4b–4g collapsed into one PR ([#304](https://github.com/ingcreators/annot/pull/304)) — plan's seven-sub-PR split was conservative, the work was uniformly mechanical. |
| [`_done/storage-error-contract.md`](./_done/storage-error-contract.md) | 2026-04-27 | Document and structure the `StorageProvider` error contract surfaced by item 8 of the 2026-04-27 friction audit. Four phases: (1) JSDoc rewrite of every `StorageProvider` method spelling out which throws / returns `undefined` / no-ops, with `@throws` clauses naming the future error classes ([#258](https://github.com/ingcreators/annot/pull/258)); (2) add `StorageError` / `StorageConflictError` / `StorageNotFoundError` / `StoragePermissionError` / `StorageQuotaError` to `@ingcreators/annot-core/storage`, re-exported from the headless surface ([#259](https://github.com/ingcreators/annot/pull/259)); (3) migrate the four first-party backends to throw the new types at conflict / not-found sites and normalise `rename*` / `move*` source-missing behaviour to throw `StorageNotFoundError` uniformly across all four backends ([#260](https://github.com/ingcreators/annot/pull/260)); (4) replace the `msg.includes("already exists")` substring match in [`split-editor-host.ts:251`](../../packages/web/src/app/split-editor-host.ts:251) with `instanceof StorageConflictError` ([#261](https://github.com/ingcreators/annot/pull/261)). Phase 3 deviated from the plan's investigation table — the table claimed all four backends already threw on missing source; in reality two silently returned and one leaked a `DOMException`. Contract test gained six structured-error assertions (×4 backends). |
| [`_done/litelement-stories-coverage.md`](./_done/litelement-stories-coverage.md) | 2026-04-27 | Retroactive Storybook coverage pass on the 16 LitElement subclasses in `packages/web/src/` that didn't ship a co-located `*.stories.ts`, taking the ratio from ~12/27 to **27/27** (every LitElement now has a story). Four phases grouped by surface area: (1) drawer sections; (2) editor header / status / filename / file-manager-shell; (3) right-panel host + sections; (4) capture / dialog / tool-flyout / toolbar + CLAUDE.md framing edit. PRs [#253](https://github.com/ingcreators/annot/pull/253)–[#256](https://github.com/ingcreators/annot/pull/256). CLAUDE.md's Storybook stance tightened from "required for new built-in Lit components" to "required for **all** built-in Lit components" — the next audit's check reduces to a symmetry assertion. |
| [`_done/lit-migration-completion.md`](./_done/lit-migration-completion.md) | 2026-04-27 | Finishes the original Lit migration ([`_done/lit-migration.md`](./_done/lit-migration.md)) by covering its deferred Phase 5c (the `toolbar.ts` flyout + save-menu orchestration) **and** the five vanilla DOM components surfaced by the 2026-04-27 friction audit: `tag-editor.ts` → `<annot-tag-editor>`, `scratchpad-section.ts` → `<annot-scratchpad-section>`, `split-editor.ts` → `<annot-split-editor>`, `gallery-page.ts` → `<annot-gallery-page>`, `gallery/context-menu.ts` → `<annot-context-menu>`. Phase 6 split into 6a (unified `#showFlyout` over `<annot-tool-flyout>` for variant + color), 6b (orchestration absorbed into `<annot-save-menu>`, `toolbar-save-menu.ts` deleted), and 6c (CLAUDE.md update + plan archive). PRs [#244](https://github.com/ingcreators/annot/pull/244)–[#251](https://github.com/ingcreators/annot/pull/251). LitElement count: 22 → 27 in `packages/web/src/`; the Storybook story ratio rose from 5/22 to ~12/27. CLAUDE.md's "stories encouraged, not required" stance tightened to "required for new built-in Lit components" going forward. |
| [`_done/vendor-libimagequant.md`](./_done/vendor-libimagequant.md) | 2026-04-27 | Replaced [`@panda-ai/imagequant`](https://www.npmjs.com/package/@panda-ai/imagequant) (2-month-old npm org with one follower, 0 GitHub stars on the upstream repo, no CI on the WASM build) with an in-tree wasm-bindgen wrapper around upstream [`ImageOptim/libimagequant`](https://github.com/ImageOptim/libimagequant) at `packages/imagequant/`. PRs [#239](https://github.com/ingcreators/annot/pull/239)–[#242](https://github.com/ingcreators/annot/pull/242) plus this Phase 5 doc cleanup. CI re-verifies the committed `pkg/` against a fresh build on every PR (`verify-wasm` job); Cargo Dependabot watches the new Rust deps; canonical build environment is Linux (Windows / macOS contributors download the Linux artefact from CI). Phase 3 deviated from the plan's byte-byte equivalence test to a structural-equivalence test — `@panda-ai/imagequant` calls libimagequant with non-default knobs we couldn't recover without source; the in-tree wrapper uses libimagequant defaults (same kernel, equal-or-better fidelity). |
| [`_done/pre-release-cleanup.md`](./_done/pre-release-cleanup.md) | 2026-04-27 | Senior-engineer audit follow-through. Stages 1 (hygiene — service-worker `catch {}` cleanup, github-store `as any` removal, Lit `annot-` prefix), 2 (`StorageProvider` capability split into `StorageWith*` interfaces + `supports*` predicates), and 4 (`core` → headless-by-construction; `property-panel` / `property-controls` / `tooltip` moved out of `core`; CI-enforced headless boundary in `packages/core/src/headless.test.ts`) all landed. Stage 3 (god-module decomposition) is partially done — toolbar.ts 3,610 → 1,604 LOC with sidecars, property-panel.ts 1,995 → 940 LOC, github-store.ts 1,818 → 1,426 LOC; selection.ts and service-worker.ts barely moved, and the schema-driven refactors landed since this plan was written produced two new god-modules (`property-schema.ts` 1,102 LOC + `tool-registry.ts` 1,007 LOC). Remaining decomposition work is being re-scoped into a fresh plan rather than continued under this one. |
| [`_done/path-based-storage.md`](./_done/path-based-storage.md) | 2026-04-27 | Dropped numeric IDs across every storage implementation. `packages/core/src/storage/path.ts` ships `ROOT_PATH` / `joinPath` / `getParentPath` / `validateName` / `uniquifyFilename`; `StorageProvider` is path-keyed; the `?p=<path>` query-parameter URL shape is canonical; the `fsFileMap` / `fsFolderMap` facade is gone; Google Drive's id↔path mapping is internal-only. Unblocked `GitHubStore` (no numeric-ID shim layer needed — see [`github-integration.md`](./github-integration.md)). All 7 phases verified by typecheck / build / test green and a manual cross-backend matrix during GitHubStore integration. |
| [`_done/pptx-export-shared-builder-finish.md`](./_done/pptx-export-shared-builder-finish.md) | 2026-04-26 | Direct follow-up to `_done/office-paste-shared-drawing-builder.md` — finishes the migration on ArrowTool's current output surface. Five phases (0–4): phase 0 pins the curved-arrow path in the PPTX golden, phase 1 drops the drop-in helper duplicates (`colorHex` / `endOOXML` / `capOOXML` / `joinOOXML` / `parseSVGPath` / EMU constants) and the legacy `tag === \"line\"` dispatch arm (past-data salvage out of scope), phase 2 routes `buildFreehandGroup`'s child emit through the shared builder (`buildFreehand` disappears), phase 3 widens `AnnotationShape` with `arrow_curve_cx` / `arrow_curve_cy` so `buildLine` and the line-helper cluster (`paintXml` / `strokeOpacity` / `_lnXml` / pptx-side `gradFillXml`) all disappear (Office paste also gains curved-arrow support for free), phase 4 cleanup + archive. PRs [#219](https://github.com/ingcreators/annot/pull/219)–[#223](https://github.com/ingcreators/annot/pull/223). After landing, `pptx-export.ts` is 877 → ~430 LOC (slide envelope + theme + content_types + ZIP packaging + freehand-group `<p:grpSp>` wrapper only); the CLAUDE.md guardrail "new tool = one `transformOf` mapping + one per-shape builder" is now literally true. |
| [`_done/office-paste-shared-drawing-builder.md`](./_done/office-paste-shared-drawing-builder.md) | 2026-04-26 | Direct follow-up to `_done/office-paste-abi-modernisation.md`. Six phases (0–5) collapsing the parallel OOXML builders (`packages/editor/src/pptx-export.ts` ~1k LOC + `packages/desktop/src-tauri/src/commands/clipboard.rs` ~780 LOC) onto a single shared TS implementation under `@ingcreators/annot-render`. Phase 0 lands a PPTX export golden, phase 1 lifts `svgAnnotationsToShapes` from the toolbar into a Tier B helper, phase 2 introduces `buildShapeXml` in `annot-render` with cross-impl byte-equivalence proof against the Rust GVML output, phase 3 collapses the Rust crate to packaging-only (Rust loses ~600 LOC, IPC contract switches to pre-built drawing XML), phase 4 migrates `pptx-export` to the shared builder (PPTX export gains sticky/callout textbox rendering for free), phase 5 cleanup + archive. PRs [#212](https://github.com/ingcreators/annot/pull/212)–[#217](https://github.com/ingcreators/annot/pull/217). After landing, a new tool means one `transformOf` mapping + one per-shape builder; PPTX export and Office paste both pick it up automatically. |
| [`_done/office-paste-abi-modernisation.md`](./_done/office-paste-abi-modernisation.md) | 2026-04-26 | Cross-language ABI cleanup. Nine phases (0–8) lifting the Office-paste TS↔Rust shape contract off legacy carrier fields: phase 0 pins a GVML XML golden snapshot via `insta`; phases 1–7 migrate `marker_shape`, `image_data_url`, `text_bg_color`, callout tail, `redact_style`, `corner_radius`, and drop unused `stroke_opacity` — each gated by byte-equivalent goldens; phase 8 deletes the one-cycle fallbacks. PRs [#202](https://github.com/ingcreators/annot/pull/202)–[#210](https://github.com/ingcreators/annot/pull/210). After landing, every TS field has a matching Rust field; no comments saying "Rust side reads from" / "carrier field". Also unlocks `marker_shape: "rounded"` (visibly rounded counter) and pasted callouts now keep their tail (`wedgeRoundRectCallout`). |
| [`_done/toolbar-highlight-flyout-kind.md`](./_done/toolbar-highlight-flyout-kind.md) | 2026-04-26 | Cleanup follow-up to `_done/toolbar-schema.md` + `_done/toolbar-apply-style-to-element.md`. Five phases lifting the last residual `if (toolId === "highlight")` branch sites in [`toolbar.ts`](../../packages/web/src/editor/toolbar.ts) + [`toolbar-canvas-menu.ts`](../../packages/web/src/editor/toolbar-canvas-menu.ts) onto new `ToolRegistryEntry` fields: `flyoutKind: "variant" \| "color"` discriminator + `chipColorForVariant` / `tooltipLabelForVariant` / `ensurePresetForVariantChange` accessors. PRs [#197](https://github.com/ingcreators/annot/pull/197)–TBD. New structural test in `packages/web/src/editor/toolbar.test.ts` greps both source files for the forbidden `toolId === "<id>"` literal — adding a future swatch-style tool (Stamp, Pen color, …) is one TOOL_REGISTRY entry, no copy-paste. |
| [`_done/toolbar-apply-style-to-element.md`](./_done/toolbar-apply-style-to-element.md) | 2026-04-26 | Closes the read/write asymmetry the toolbar's schema migration left open. Four phases adding `applyStyleToElement?: (el, preset) => void` to `ToolRegistryEntry` as the inverse of the existing `extractStyleFromElement`, then collapsing `applyPresetStyleAttrs` / `applyMarkerPresetStyle` / `applyTextboxPresetStyle` (~120 LOC of element-tag cascade) into a generic registry dispatch. PRs [#193](https://github.com/ingcreators/annot/pull/193)–[#196](https://github.com/ingcreators/annot/pull/196). Structural test in `tool-registry.test.ts` enforces every extractor has a paired writer; `toolbar-preset-helpers.ts` shrank from 322 → 122 LOC. |
| [`_done/tool-property-renderer-schema.md`](./_done/tool-property-renderer-schema.md) | 2026-04-26 | Closing piece of the schema-driven trilogy. Five phases declarativising the Tool-side per-tool property panel: Tier B `panelControls` per `TOOL_REGISTRY` entry, Tier B `TOOL_PANEL_ADAPTERS` writing onto `ToolOptions`, `selectionDefMetadata` bridging label / options / ranges / `allowNone` from the SELECTION registry. PRs [#188](https://github.com/ingcreators/annot/pull/188)–[#192](https://github.com/ingcreators/annot/pull/192). The 649-LOC imperative cascade in `tool-property-renderer.ts` is gone; adding a new tool control is one entry in the registry + one matching adapter. |
| [`_done/toolbar-schema.md`](./_done/toolbar-schema.md) | 2026-04-26 | Sibling to `_done/property-panel-schema.md`. Six phases applying the same registry-driven pattern to `packages/web/src/editor/toolbar.ts`. Tier B `TOOL_REGISTRY` consolidates id / label / icon / variants / preset fields / element classifier / style extractor; generic `presetToWire` / `presetFromWire` replace the manual ~25-field marshallers; Tier C `tool-factories.ts` holds `CanvasManager`-bound factories. `toolbar-variants.ts` deleted. PRs [#166](https://github.com/ingcreators/annot/pull/166)–[#171](https://github.com/ingcreators/annot/pull/171). `toolbar.ts` shrank from 2,181 → ~1,996 LOC. |
| [`_done/property-panel-schema-extensions.md`](./_done/property-panel-schema-extensions.md) | 2026-04-26 | Follow-up to `_done/property-panel-schema.md`. Three phases extending the registry to cover the residual imperative rows: marker bg-primitive controls (#162), shape transparency / cap type + arrow-aware augmentations (#163), per-end arrow type+size pulldowns with dynamic `getOptions` (#164). `property-panel.ts` shrank from 1,377 → 991 LOC across the series; total reduction across the schema-driven migration + extensions is 826 LOC vs the original 1,817 baseline. |
| [`_done/property-panel-schema.md`](./_done/property-panel-schema.md) | 2026-04-26 | Schema-driven render for `PropertyPanel` — declarative `PROPERTY_CONTROLS` registry in Tier B + free-function renderer in Tier C. Closing piece of the testability series (proposal 8). Phases 1–4 landed in [#153](https://github.com/ingcreators/annot/pull/153)–[#161](https://github.com/ingcreators/annot/pull/161). Every category in `CATEGORY_CONTROL_SHAPE` now routes through `#renderViaRegistry` → `renderControl`; `property-panel.ts` shrank from 1,817 → ~1,580 LOC, with the residual being imperative rows the registry doesn't yet model (transparency, cap type, per-end arrow grids, marker bg-primitive). |
| [`_done/three-package-split.md`](./_done/three-package-split.md) | 2026-04-26 | Split `core/editor/` into TWO new workspace packages: `@ingcreators/annot-editor` (live-browser editor primitives) and `@ingcreators/annot-render` (data-driven `ImageRecord` rendering — gallery bulk-export's future home). Phase 0 scaffold + Phase 1–8 moves + Phase 9 docs landed in [#128](https://github.com/ingcreators/annot/pull/128)–[#137](https://github.com/ingcreators/annot/pull/137). CI-enforced cycle invariant in `packages/core/src/headless.test.ts` keeps `annot-core` decoupled from the editor / render packages. |
| [`_done/source-audit-cleanup.md`](./_done/source-audit-cleanup.md) | 2026-04-25 | Repo-hygiene + presentation pass for corporate adoption auditing. All seven phases shipped in [#100](https://github.com/ingcreators/annot/pull/100)–[#106](https://github.com/ingcreators/annot/pull/106): Apache-2.0 LICENSE + audit-facing root docs, plan-status hygiene, centralised logger shim, `any` triage in toolbar.ts and outside, `assertNonNull` helper + fragile DOM-lookup guards, README "for evaluators" section. |
| [`_done/lit-migration.md`](./_done/lit-migration.md) | 2026-04-25 | Multi-phase migration of `packages/web` UI from imperative DOM to Lit Web Components. All seven phases (0–6) shipped in [#85](https://github.com/ingcreators/annot/pull/85)–[#93](https://github.com/ingcreators/annot/pull/93); follow-up corner-case fixes in [#94](https://github.com/ingcreators/annot/pull/94)–[#98](https://github.com/ingcreators/annot/pull/98). |
| [`_done/storybook-introduction.md`](./_done/storybook-introduction.md) | 2026-04-25 | Storybook bootstrap in `packages/web` as the component showroom + visual-regression net for the Lit migration. Phase 1 landed in [#84](https://github.com/ingcreators/annot/pull/84); Phase 2 (CI-blocking) and beyond are explicitly optional follow-ups. |
| [`_done/plugin-ui-slots.md`](./_done/plugin-ui-slots.md) | 2026-04-25 | Drawer + right-panel become generic `UISection` hosts. Built-ins migrated; plugins register via split `addDrawerSection` / `addRightPanelSection`. Phases 1–3 landed in [#80](https://github.com/ingcreators/annot/pull/80)–[#82](https://github.com/ingcreators/annot/pull/82); optional Phase 4 polish deferred. |
| [`_done/plugin-sidebar-tabs.md`](./_done/plugin-sidebar-tabs.md) | 2026-04-25 | Sidebar "Views" section + section-priority ordering + setter-based tab API + built-in "Recent" tab. Phase 1 landed in [#78](https://github.com/ingcreators/annot/pull/78); optional Phase 2 visual polish deferred. |
| [`_done/plugin-storage-registration.md`](./_done/plugin-storage-registration.md) | 2026-04-25 | Open `storage/bridge.ts` to plugin-registered backends so `annot-cloud`'s pointer-commit store can land without a fork. Phases A, B, C landed in [#74](https://github.com/ingcreators/annot/pull/74)–[#76](https://github.com/ingcreators/annot/pull/76). |
| [`_done/app-decomposition.md`](./_done/app-decomposition.md) | 2026-04-25 | Break `packages/web/src/app.ts` (2.6k lines) into collaborator modules and ship a `PluginHost` MVP. Phases 0–5 + a 3.5 follow-up landed in [#65](https://github.com/ingcreators/annot/pull/65)–[#72](https://github.com/ingcreators/annot/pull/72); the three Phase-5 readiness-gate items each got their own follow-up plan (also landed). |

## Plan lifecycle

- **Draft** — being discussed, not yet decided.
- **Queued** — approved, waiting for implementation window.
- **In progress** — actively being implemented; link to tracking
  branch / PR if any.
- **Done** — landed; move the file to `_done/` and leave a one-line
  pointer in the table above if it's historically important.
- **Abandoned** — move to `_done/` with an `ABANDONED:` prefix in
  the file header explaining why.

## Adding a plan

Start the plan file with this header:

```markdown
# <title>

> **Status:** Draft | Queued | In progress | Done | Abandoned
> **Compatibility:** Which packages / systems are affected.
> **Risk:** Single landing vs phased, data migration needs,
>           breaking changes.

## Context
...

## Design
...

## Phased plan
...

## Verification
...

## Migration notes
...
```

Keep plans self-contained — future readers (including Claude Code
after context reset) should be able to execute from the plan alone
without chasing through chat history. Include:

- Rationale for non-obvious design choices.
- File paths that will be touched.
- Forward-looking notes (e.g. "how this enables X later").
